# 화학사고 지자체 대응 지원도구

화학사고 발생 시 지자체 담당자의 초기 대응 업무를 돕는 정적 HTML 도구 모음입니다.
**별도 서버 없이** 홈페이지 게시 또는 파일 배포만으로 동작합니다.

| 도구 | 상태 |
|---|---|
| ① 주민대피 문자 작성 | **초안 (v0.3.0-draft)** |
| ② 인근 수계 누출 판단 | 준비 중 |
| ③ 폐기물 처리업체 섭외 | 준비 중 |

---

## ① 주민대피 문자 작성 도구

대피·주민안내 **발송 지시를 받은 뒤** 사용하는 문안 작성 도구입니다.
대피 여부·대상지역·발송단계의 판단은 도구가 하지 않습니다.

### 화면

한 화면에서 끝나도록 **왼쪽 입력 · 오른쪽 문안** 2단으로 배치했습니다.
입력하는 동안 오른쪽 문안이 실시간으로 바뀌므로 화면을 오르내릴 필요가 없습니다.

```
┌─ 발송 단계 4개 (가로) ────────────────────────────┐
│ 실내대피 알림 │ 주민소산 │ 실내대피 종료 │ 대피명령 종료 │
└───────────────────────────────────────────────────┘
┌─ 사고정보 (입력) ──────┐ ┌─ 생성 문안 (고정) ────┐
│ 사고유형 칩 4개        │ │ 검증 결과              │
│ 기본정보 7칸           │ │ 긴급재난문자 · 90자    │
│  └ 물질정보 요약(접힘) │ │ 자체 시스템            │
│ 대피장소·집결지        │ │ …                      │
└────────────────────────┘ │ 복사 전 3항목 확인     │
                           └────────────────────────┘
```

- 화면이 시스템 다크모드를 따라가지 않습니다. 급할 때 판독성이 떨어지므로
  **밝고 대비가 높은 화면 하나로 고정**했습니다.
- 물질정보는 요약 한 줄만 보이고, 필요할 때만 펼칩니다.
- 대피장소 목록은 "찾기" 버튼을 눌렀을 때만 열립니다.
- 980px 미만에서는 세로로 쌓이고, 모바일에서도 가로 스크롤이 없습니다.

### 기능

- 「화학사고 재난문자 표준(안)」 12개 문안 자동 조합
- **사고유형 4종**(누출·화재·폭발·기타)에 따라 사고 표현이 자동으로 바뀜
- **물질정보 460종 참고 패널** — 물질명·유사명·CAS로 검색하면 성상·냄새, 증기밀도,
  화재·폭발 가능성, 사고유형별 참고 거리, 증상, 응급조치를 표시
- 사고 1건 = 한 세션. 단계만 바꾸면 사고정보가 승계됨 (`sessionStorage`, 브라우저 닫으면 소멸)
- 한 단계에서 **사고발생 지역용·그 외 지역용**을 함께 생성
- 긴급재난문자 90자 초과 시 우선순위 축약 + **무엇을 뺐는지 표시**
- 전국 화학사고 대피장소 1,849곳 시·군·구별 선택 (오타·미등록 장소 방지)
- 한글 조사 자동 처리 (`황산이` / `염소가스가`, `경로당으로` / `학교로`)
- 사고 시·군이 발송기관과 같으면 자동 생략해 글자수 절약
- 필수 미입력, 예시값 잔존, 실내대피↔외부대피 혼재, 지역 불일치 검사
- 복사 전 3항목 확인 체크 (미체크 시 복사 버튼 비활성)
- 생성 기록 TXT 저장 (서버 로그가 없으므로 사후 확인용)
- 실제 발송 사례 42건 참고 패널 (비승인 자료로 명시)
- 입력칸에 예시값을 두지 않음 — 예시를 실제 값으로 착각해 발송하는 사고 방지
- 추가문의 전화번호는 문구에 넣지 않음 — 사고 시 문의 폭주로 대응이 어려워지는 것 방지

### 여는 방법

**홈페이지 게시** — 저장소 전체를 웹 경로에 올리고 `index.html` 로 연결

**오프라인 단일 파일 (망분리 행정망 PC용)**
`dist/화학사고_주민대피문자_작성도구.html` 파일 **하나만** 옮기면 됩니다.
외부 참조 0건이라 인터넷 없이 브라우저에서 바로 열립니다.

---

## 구조

```
index.html            도구 모음 진입 (3종 공통 셸)
assets/shell.css      공통 스타일
sms/index.html        주민대피 문자 도구 화면
sms/app.js            문안 조합·축약·검증 로직
data/version.js       기준 문서·글자수 상한·관리부서   ← 갱신 시 여기부터
data/templates.js     표준(안) 12개 문안
data/shelters.js      대피장소 1,849건 (자동 생성)
data/materials.js     물질정보 460종 (자동 생성)
data/stats.js         사고통계 2015~2025 (자동 생성)
data/cases.js         실제 발송 사례 42건 (자동 생성)
source/               원자료 (PDF·HWPX·XLSX·XLS)
build/hwpx.py         HWPX 표 구조 파서
build/extract_mat.py  물질정보 hwpx → 구조화 JSON
build/make_data.py    원자료 → data/*.js 재생성
build/build_single.py 단일 HTML 빌드
dist/                 배포용 단일 파일
docs/                 분석·변경점 기록
```

데이터를 `.json` 이 아닌 `.js` 로 둔 이유: `file://` 로 열었을 때 `fetch()` 는 차단되지만
`<script src>` 는 동작하므로, 망분리 환경에서도 모듈 구조 그대로 쓸 수 있습니다.

## 갱신 방법

**문안이 바뀌면** — `data/templates.js` 수정 → `data/version.js` 의 `문안근거`·`반영일` 갱신
→ `python3 build/build_single.py`

**데이터가 갱신되면** — `source/` 의 원본 파일 교체 → `build/make_data.py` 의 파일명·기준일 수정
→ `python3 build/make_data.py` → `python3 build/build_single.py`
→ `data/version.js` 의 출처·기준일 갱신

```bash
pip install openpyxl xlrd
python3 build/make_data.py      # shelters · materials · stats · cases 전부 재생성
python3 build/build_single.py   # dist/ 단일 파일 빌드
```

---

## 초안 단계에서 확인이 필요한 사항

이 초안은 **업로드된 5개 원자료만으로** 만들었습니다. 다음은 아직 확정되지 않았습니다.

1. **글자수 산정 방식** — 실제 재난문자방송시스템과 대조 필요.
   기관명 자동 부기분이 90자에 포함되는지 등. 대조 후 `data/version.js` 의
   `글자수_검증여부` 를 `true` 로 변경하세요.
2. **망분리 여부** — 지자체가 어느 망에서 문안을 작성·송출하는지에 따라
   배포 형태(홈페이지 / 단일 파일)가 결정됩니다.
3. **표준(안) 원문 교정 5건** — `docs/표준안_대비_변경점.md` 참조. 원내 검토 필요.
4. **관리부서·연락처** — `data/version.js` 에 기재해야 합니다.
5. **기관명 위치** — 표준(안)은 문구 앞, 실제 사례는 문구 끝이 다수. 방침 결정 필요.
6. **사고유형별 문안 표현** — 화재·폭발·기타 문구는 표준(안)에 규정이 없어
   이 도구가 새로 만든 것입니다. 원내 검토 필요.
7. **물질별 행동요령** — 현재 도구는 물질에 따라 행동요령을 **바꾸지 않습니다**.
   표준(안) 행동요령을 그대로 쓰고, 물질정보는 참고 표시와 경고로만 제공합니다.
   물질별 행동요령을 문안에 넣을지는 별도 검토가 필요합니다(90자 제약과 충돌).

---

본 도구는 재난문자 **문안 작성을 지원하는 참고도구**입니다.
대피 여부·대상지역·발송단계의 판단과 최종 발송 책임은 해당 재난관리기관에 있습니다.
