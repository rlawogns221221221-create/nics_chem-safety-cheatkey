__pycache__/
*.pyc
node_modules/
.DS_Store

# ── 개인정보가 든 파일은 저장소에 올리지 않습니다 ──────────────
# 방제자원 내부판 — 업체 담당자 이름·직통번호가 들어 있습니다.
# 내부망 공유폴더로만 전달하고, 메일·외부 저장소로 내보내지 마세요.
data/resources.internal.js
dist/*_내부용.html

# ── 배포용 결과물 ────────────────────────────────────────────
# build/make_release.py 가 만드는 것들입니다. 언제든 다시 만들 수 있고,
# 저장소에 두면 같은 내용이 두 벌이 되어 어느 쪽이 최신인지 헷갈립니다.
release/
*_배포용.zip
