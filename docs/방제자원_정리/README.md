# 방제자원 원자료 정리 — 여기부터 읽으세요

구글드라이브 **`방제자원_전체`** 폴더에 있던 자료를 한 줄 한 줄 펴서
**표 18개**로 만들었습니다. 서비스(`data/`)에는 **아직 반영하지 않았습니다.**

| 무엇 | 어디 |
|---|---|
| **눈으로 훑어보기** | `한눈에보기.html` ← 브라우저로 여세요 (표 18개를 한 화면에서 검색) |
| 표 파일 | `표/*.csv` (엑셀로 열립니다) |
| 발표용 한 장 보고 | `../화학사고_방제자원_취합현황.pdf` |
| **서비스에 어떻게 녹일지** | `반영방안.md` |
| 세어 본 수치 | `통계.json` |

---

## 1. 한 문단 요약

원자료는 **폴더 5개·하위폴더 40여 개·파일 100여 개**에 흩어져 있었고,
기관마다 양식이 달라 한 파일 안에 표가 여러 개 들어 있거나 칸 수가 중간에
바뀌는 것도 있었습니다. 이것을 **표 18개 · 자료 9,226행**으로 폈습니다.
지금 서비스에 들어 있는 방제자원은 **470건**인데, 이 자료로 **1,947곳**
(약 4.1배)을 지도에 올릴 수 있습니다. **`hwp`·구형 `xls` 까지 전부 열어**
"건수만 아는 자료"는 이제 없습니다 — 전부 한 줄 한 줄 갖췄습니다.

⚠ 원자료에는 **담당자 실명 2,719건 · 개인 휴대전화 495건 · 사업자등록번호
4,277건**이 들어 있었습니다. 이 저장소는 공개되므로 **전부 걷어냈습니다**
(`표/99_개인정보_제거현황.csv`). 기관·사업장 **대표번호만** 남겼습니다.

---

## 2. 무엇이 들어왔나 — 표 18개

### 곧바로 쓸 수 있는 것 (주소·전화가 있어 지도에 찍힘)

| 표 | 행 | 실제 지점 | 무엇 | 지금 서비스 |
|---|---:|---:|---|---|
| `01` 지자체 보유 방재장비 | 437 | **437곳** (218개 지자체) | 시·군·구가 직접 가진 차량·보호구·중화제 | 이미 427건 반영 |
| `03` 지자체 관할 폐기물·중장비 업체 | 763 | **496개 업체** | 사고 때 부를 수 있는 민간 탱크로리·덤프·굴착기 | **없음 → 신규** |
| `04` 청센터 방제장비함 | 492 | **48개 설치장소** | 산단·고속도로에 실제로 놓여 있는 장비함 | **없음 → 신규** |
| `05` 사업장 방제물품(화학안전공동체) | 1,296 | **106개 사업장** | 사업장이 자체 보유한 방제물품·방재인원 | **없음 → 신규** |
| `07` 해경 광역방제지원센터 비축물자 | 21 | 3개 기지 | 항만 비축 — 개인보호구 6.7만 세트 | 3건 반영 |
| `08` 해경 화학방제정 | 25 | 25척 | 해상 화학사고 출동 선박 | **없음 → 신규** |
| `09` 수질오염방제센터 보유장비 | 54 | 4개 권역 | Level C 보호구·케미컬흡착재 등 | 4건 반영 |
| `10` ERaCV 비상캡슐 보유업체 | 19 | 19곳 | 누출 봄베를 통째로 담아 옮기는 캡슐 | 19건 반영 |
| `11` 중합방지제 판매업체 | 3 | 3곳 | 중합 폭주를 막는 약품 | **없음 → 신규** |
| `14` 폐기물처리업체 (전국취합) | 1,505 | **1,123개 업체** | ★ 시·도/시·군·구가 나뉜 가장 깔끔한 자료 · 전화 99.8% | 15건 반영 |
| `15` 전남·광주 방제약품·크레인 | 11 | 11곳 | ⚠ 자료 기준 **'17년** — 확인 필요 | **없음 → 신규** |
| `16` 전국 독성가스 회수업체 | 2 | 2곳 | 자료에 원래 두 곳뿐 | 2건 반영 |
| | | **합계 1,947곳** | | 현재 470건 |

> `03` 의 폐기물업체 330곳은 `14` 의 전국취합에 겹치므로 합계에서는
> `03` 의 **중장비 166곳만** 셌습니다.

### 폐기물 자료는 두 갈래로 들어왔습니다 — 둘을 견주어 보세요

| 표 | 수 | 무엇 |
|---|---:|---|
| `14` 폐기물처리업체 **전국취합** | **1,505행 / 1,123곳** | 인허가 기준 전국 취합본. 시·도/시·군·구가 나뉘고 전화가 99.8% 있습니다. **서비스에 넣을 것은 이쪽입니다** |
| `12` 유역청별 회신 건수 | 1,669곳 | 유역청 7곳이 각자 낸 회신의 **건수만** 집계. 의료폐기물 수집운반(278곳)이 포함돼 있어 `14` 와 범위가 다릅니다 |
| `13` 지정폐기물 · 대구청 | 239곳 | 유역청 회신 가운데 행 단위로 받은 것 |

> 두 수(1,505 ↔ 1,669)가 다른 것은 **자료 기준일과 포함 범위가 달라서**입니다.
> 어느 한쪽이 틀린 것이 아닙니다.

### 자원이 아니라 '위험시설'

| 표 | 행 | 지점 | 무엇 |
|---|---:|---:|---|
| `06` 독성가스 취급사업장 | 4,277 | **1,579개 사업장** | 사고가 **날 수 있는** 곳. 물질 256종 (암모니아 1,774건·염소 400건) |

> ⚠ **`06`은 방제자원이 아닙니다.** 자원 목록에 섞으면 "여기 가면 도움을
> 받는다"로 잘못 읽힙니다. 쓰려면 **별도의 층**으로 다뤄야 합니다
> (`반영방안.md` 5절).

### 그 밖

- `00_원자료_목록.csv` — 드라이브에 무엇이 있고 **무엇을 못 읽었는지**
- `02_지자체_보유방재장비_청센터별.csv` — `01`과 같은 자료를 유역청 관할로 묶은 판 (75행 · 7개 센터만)
- `99_개인정보_제거현황.csv` — 걷어낸 개인정보 내역

---

## 3. 자료의 상태 — 그대로 쓰면 안 되는 것들

정리하면서 실제로 걸린 것만 적습니다.

1. **개인정보가 곳곳에 있습니다.** 특히 `읍면 방제장비 및 중장비업체 현황`
   (진천군)은 **연락처가 전부 개인 휴대전화**라 걷어내면 남는 것이 이름뿐
   입니다. 이 파일은 정리본에 넣지 않았습니다.
2. **좌표가 하나도 없습니다.** 전부 주소 문자열뿐입니다. 지도에 찍으려면
   지오코딩이 필요합니다(현재 서비스도 주소로 어림잡고 있고, 470건 중
   **정확한 좌표는 0건**입니다).
3. **업종 이름이 제각각입니다.** `03`의 업종만 봐도 `폐기물 수집·운반업체` /
   `폐기물수집운반업` / `수집운반업` / `지정폐기물 수집운반업체` 가 따로
   있습니다(19가지). 묶어 주지 않으면 사용자가 같은 것을 네 번 봅니다.
   그리고 **눈에 안 보이는 글자가 검색을 막고 있었습니다** — `14` 의
   `수집〮운반업` 에서 가운뎃점처럼 보이는 것은 사실 **U+302E(한글 방점)**
   이라, "수집운반"으로 찾으면 566건 중 **1건만** 나왔습니다. `03` 에는
   U+2219(수학 기호 ∙)가 쓰였습니다. 정리본에서는 전부 `·` 로 맞췄습니다
   (`build/방제자원_정리.py` 의 `글자다듬기`).
4. **전화번호가 비어 있는 곳이 있습니다.** `03`은 763행 중 **148행(19%)**
   에 대표번호가 없습니다.
5. **자료 기준일이 기관마다 다릅니다.** 금강청 2024-12 ↔ 낙동강청 2026-01.
   화면에 한 날짜만 적으면 거짓이 됩니다.
6. **같은 파일이 여러 폴더에 중복**돼 있습니다(해경 비축기지는 3곳에
   같은 파일). `2-5. 민간 방제 선박.xlsx` 는 **이름과 내용이 다릅니다**
   — 열어 보면 해경 비축기지 자료입니다. 해경 hwp/hwpx 4건도
   이미 xlsx 로 있는 것과 같은 내용이었습니다.
   그리고 `15` 의 전남·광주 자료는 **기준이 2017년**이라 9년 지났습니다.
7. **못 읽은 파일은 이제 1건뿐입니다** — 155MB PDF(경기 비상대응정보집).
   `.hwp`/`.hwpx` 6건과 구형식 `.xls` 1건은 **모두 열었습니다**
   (파일을 내려받아 `build/xlsx읽기.py`·hwp 해독으로 처리).
   그 결과 **폐기물처리업체 1,505행**과 **방제약품 11곳**이 새로 들어왔습니다.
   ⚠ **'가스 회수' 갈래가 2건인 것은 파일을 못 읽어서가 아니라
   자료에 원래 두 곳뿐이기 때문**이었습니다(`16` 참고).

---

## 4. 다시 만들려면

```bash
python3 build/방제자원_정리.py    # 원자료 → 표/*.csv
python3 build/방제자원_통계.py    # 표 → 통계.json
python3 build/방제자원_보기.py    # 표 → 한눈에보기.html
node   build/방제자원_보고서.mjs   # 발표용 한 장 보고서 PDF (한 쪽을 넘기면 멈춤)
```

원자료 원문은 개인정보 때문에 저장소에 넣지 않았습니다. 드라이브 파일 ID 는
`표/00_원자료_목록.csv` 에 적어 두었습니다.
