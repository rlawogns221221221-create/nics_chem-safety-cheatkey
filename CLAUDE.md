# 함께 일하는 규칙 — 화학사고 지자체 대응 지원도구

새 대화에서 이어 작업할 때 **이 파일을 먼저 읽으세요.** 무엇을 만드는 중인지,
무엇을 지켜야 하는지, 어디까지 왔는지가 여기 있습니다.
자세한 기능 설명은 `README.md`, 결정의 배경은 `docs/` 를 보세요.

작업 가지(branch): **`claude/chemical-accident-evacuation-sms-hsgoit`**

---

## 1. 무엇을 만드는가

화학사고가 났을 때 **지자체 담당자**가 쓰는 업무도구 3종과 그 셋을 고르는 첫 화면.
발주는 화학물질안전원. 급한 상황에서 몇 초 안에 답을 얻는 것이 목적입니다.

| | 도구 | 폴더 |
|---|---|---|
| ① | 주민대피 문자생성기 — 사고정보를 넣으면 재난문자 표준문안이 나옴 | `sms/` |
| ② | 주민 대피장소 찾기 — 사고지점 주변 대피처를 거리·도보시간과 함께 찾음 | `map/` |
| ③ | 방제 물품·장비 찾기 — 필요한 것을 고르고 사고지점을 넣으면 주변에서 찾음 | `res/` |
| | 진입 화면 | `index.html` |

⚠ 위 ①②③ 은 **폴더 이름표**일 뿐입니다. 진입 화면의 카드 차례는 다릅니다 —
`01 방제 물품·장비 찾기 → 02 주민 대피장소 찾기 → 03 주민대피 문자생성기`
(7절). 헷갈리기 쉬우니 화면을 고칠 때는 카드 차례를 보세요.

---

## 2. 무엇보다 먼저 지킬 것

이 다섯 가지는 기능보다 앞섭니다. 어기면 사람이 잘못된 자리로 갑니다.

1. **도구는 판단하지 않습니다.** 어느 대피장소가 적절한지, 어디까지 대피시킬지는
   담당자가 정합니다. 도구는 자료를 모아 보여줄 뿐입니다.
2. **어림값은 어림값이라고 화면에 적습니다.** 직선거리·도보시간·행정경계로 잡은
   좌표에는 `대략`·`직선거리로 어림한 값` 같은 표시를 답니다. 실제로 잰 값과
   눈으로 구분되지 않으면 안 됩니다.
3. **없는 값은 지어내지 않습니다.** 좌표 없는 시설은 지도에 찍지 않고, 확인 못 한
   전화번호는 비워 둡니다(기억나는 대로 적었다가 틀린 적이 있습니다).
4. **개인정보를 넣지 않습니다.** 업체 담당자 이름·휴대전화는 공개판에 절대 넣지
   않습니다. 대표번호·부서번호만 씁니다. `build/make_release.py` 가 기계로 검사해
   하나라도 걸리면 배포 폴더를 만들지 않습니다.
5. **화면에 새로운 '주장'을 얹기 전에 사용자에게 묻습니다.** 자료를 보여 주는 것과
   "실제로는 이렇습니다"라고 단정하는 것은 다릅니다.
   (실제로 그런 문구를 넣었다가 되돌린 적이 있습니다 — 아래 7절)

그리고 **모든 것을 한국어로** 씁니다. 화면·주석·커밋 메시지 전부. 주석은 "무엇을"
말고 **"왜 이렇게 했는지"** 를 적습니다.

---

## 3. 어떻게 만들어져 있나

```
index.html        진입 화면
sms/ map/ res/    도구 3종 (index.html + app.js)
assets/
  shell.css       화면 전체의 유일한 스타일시트
  mapcore.js      지도 공용 엔진 — 투영·거리·타일·카메라·자료읽기
  online.js       인터넷이 있어야 되는 것만 (장소검색·역지오코딩·길찾기)
  shmap.js        ① 안의 '지도에서 고르기' 창
  matpicker.js    물질 고르기
  krds/           KRDS 공식 디자인 토큰 (원본 그대로 — 고치지 말 것)
  fonts/          Pretendard GOV — KRDS 표준 서체 (Regular · Bold)
  theme.js        밝음 ↔ 고대비(어두움) 바꾸기 — head 에서 부릅니다
  portal.css      진입 화면 전용 스타일시트 (도구와 같은 토큰을 씁니다)
  portal.js       진입 화면 전용 — 머리띠 사진 넘김 멈춤 단추 · iOS :active
  pwa.js          바탕화면 아이콘 안내 · 예전 서비스워커 지우기(7절)
  img/icon-*.png  바탕화면 아이콘 (build/make_icons.mjs 가 구움)
manifest.webmanifest  웹앱 이름·아이콘·시작 주소  ← 사이트 뿌리여야 함
sw.js             예전에 깔린 서비스워커를 지움 ← 뿌리여야 함 · 지우지 말 것
data/             화면이 읽는 자료 (기계 생성 — 손으로 고치지 말 것)
                  resources2.js 가 ③ 이 읽는 두 갈래 자료입니다
                  (예전 resources.js 는 화면이 부르지 않습니다)
build/            자료 생성·빌드 스크립트
tests/            화면 검증 (실제 브라우저로 눌러 봄)
docs/사용설명서.html  지자체 담당자용 설명서(8쪽·그림 위주) **원본** — PDF 를 고치지 말 것
docs/원페이퍼.html    한 장 요약(A4 1쪽) — 붙여 두고 보는 것 · 한 쪽을 넘기면 빌드가 멈춤
docs/방제자원_정리/   드라이브 원자료를 편 것 — 표 19개·한눈에보기.html·반영방안 (8절)
                     표 17 은 사용자가 보낸 허가증 24건을 읽어 만든 것 (8절)
docs/방제자원_정리/통합/  사용자 분류안(PPT)대로 두 갈래로 묶은 것 —
                     업체.csv(1,582곳) · 물품.csv(4,075줄) · README.md (8절)
docs/방제자원_현황보고.html  발표용 한 장 보고 **원본** — 한 쪽을 넘기면 빌드가 멈춤
docs/임시주거시설_받는법.md  ② 의 임시주거시설 층을 켜려면 무엇을 하면 되는지
                     (국내 IP 에서만 받아짐 — 두 번 확인한 근거까지)
docs/원내자료_공부와_최종방향.md  원내 자료 3건을 읽고 정리한 공식 정의·기준과
                     서비스 최종 방향(안) — **③ 을 고치기 전에 읽을 것** (8절)
preview/          사용자가 눌러 보는 확인용 진입 화면 (Artifact 로 올림 · 5절)
dist/             망분리 PC용 단일 HTML (build_single.py 가 만듦)
release/          다른 기관에 넘길 폴더 (make_release.py 가 만듦 · git 제외)
source/           원자료 (git 에 있음 · 배포에서는 빠짐)
```

**코드 규칙**

- **빌드 도구·프레임워크·npm 을 쓰지 않습니다.** 브라우저가 바로 읽습니다.
- **ES5 문법**으로 씁니다(`var`, `function`). 화살표 함수·`let`·템플릿 문자열을
  쓰지 않습니다 — 행정망의 낡은 브라우저에서 열립니다.
  (`tests/` 안의 검증 스크립트만 예외로 최신 문법을 씁니다)
- 모듈이 없습니다. 각 `app.js` 는 `(function () { "use strict"; … })();` 한 덩어리이고,
  공용 기능은 `window.MAPCORE` · `window.ONLINE` 으로 주고받습니다.
- **`file://` 로 열어도 그대로 돌아야 합니다.** 서버가 필요한 코드를 넣지 마세요.
- 인터넷이 필요한 기능은 **`assets/online.js` 에만** 둡니다. 그 파일이 없어도
  화면은 전부 동작해야 합니다(망분리 PC용 단일 파일에서는 실제로 빠집니다).
- **색·글자·간격은 KRDS 토큰(`assets/krds/krds_tokens.css`)에서 가져옵니다.**
  값을 코드에 직접 쓰지 마세요. 어떤 토큰이 있는지는 `docs/krds_요약.md` 참고.
  그 파일은 정부 공식 원본이라 **손으로 고치지 않습니다** — 바꿔야 하면 우리
  별칭 변수를 만들어 덮습니다.
- 손가락으로 누르는 것은 `@media (pointer: coarse)` 에서 40px 이상.
- **크기도 KRDS 계단을 씁니다.** 글자 `--fs-xs/s/m/l`(13·15·17·19px), 컨트롤
  높이 `--h-s/m/l`(40·48·56px), 모서리 `--r-s/r/r-l`(4·6·10px).
  뿌리 글자크기가 62.5% 라 `1.7rem = 17px` 입니다.
- **우리 별칭 이름은 밝은 `:root` 와 고대비 블록 **양쪽에** 넣습니다.**
  한쪽만 넣으면 다른 모드에서 그 이름을 쓰는 CSS 줄이 **조용히 통째로
  버려집니다**(테두리가 아예 사라지는 식). `tests/run.sh tok` 이 검사합니다.
- **색을 하드코딩하지 마세요.** 고대비(어두운) 모드가 토큰만 갈아끼우는
  방식이라, 직접 적은 색은 어두운 화면에서 안 바뀌어 글자가 사라집니다.
  지도 위 반투명 패널은 `--glass`, 짙은 띠 위 글자는 `--on-deep` 을 씁니다.

---

## 4. 돌려 보고 검증하기

```bash
tests/run.sh              # 19개 묶음 모두 (12분쯤)
tests/run.sh t8 map       # 고른 것만
tests/run.sh -l           # 묶음 목록 보기
```

실제 Chromium 을 띄워 화면을 눌러 보고 확인합니다. **화면을 고쳤으면 관련 묶음을
돌리고, 커밋 전에는 모두 돌립니다.** 배경지도 타일을 못 받아 나는
`ERR_TUNNEL_CONNECTION_FAILED` 는 정상입니다(이 환경은 바깥으로 못 나갑니다).

자료·빌드:

```bash
python3 build/make_data.py       # source/ → data/ (대피장소·물질·통계·사례)
python3 build/make_resources.py  # 예전 방제자원 자료 (화면은 부르지 않음)
python3 build/make_resources2.py # 통합 csv → data/resources2.js  ← ③ 이 읽는 자료
python3 build/build_single.py    # dist/ 단일 파일 3개
python3 build/make_release.py    # release/ 배포 폴더 + zip
node   build/make_icons.mjs      # build/icon.svg → 바탕화면 아이콘 PNG 3종
node   build/make_guide.mjs      # 설명서 PDF 두 가지 (8쪽 + 한장요약) · 사진도 다시 찍음

python3 build/방제자원_정리.py    # 드라이브 원자료 → docs/방제자원_정리/표/*.csv
python3 build/방제자원_통계.py    # 표 → 통계.json (보고서 수치는 여기서 나옵니다)
python3 build/방제자원_통합.py    # 표 → 통합/업체.csv · 물품.csv (분류안의 두 갈래)
python3 build/make_resources2.py  # 통합 csv → data/resources2.js (③ 이 읽는 자료)
python3 build/방제자원_통합_보기.py # 통합 → 한눈에보기.html (검토용)
python3 build/방제자원_보기.py    # 표 → 한눈에보기.html
node   build/방제자원_보고서.mjs   # 발표용 한 장 보고서 PDF
python3 build/hwp읽기.py 파일.hwp  # hwp·hwpx 에서 글자 뽑기 (한 번 쓰는 도구)
```

**지자체에 링크로 나눠 주는 법은 `docs/배포하기.md`** 에 있습니다 —
어디에 올릴지(계정 이름이 주소에 안 보이게), 올린 뒤 확인할 두 가지,
담당자에게 그대로 보낼 안내문까지 적어 두었습니다.
**지금 그 링크는 닫혀 있습니다** — 막는 법·다시 여는 법은
`docs/링크막기.md` 입니다(7절).

**자료를 고칠 때는 `data/*.js` 를 손으로 고치지 말고** `source/` 원자료를 바꾼 뒤
스크립트를 다시 돌립니다. 화면을 고쳤으면 `build_single.py` 로 `dist/` 도 다시
만드세요 — `tests/t8` 이 그 파일을 검사합니다.

---

## 5. 사용자가 눌러 보는 확인용 링크 (Artifact)

사용자는 개발자가 아니라 저장소를 내려받아 열지 않습니다. 그래서 만든 화면을
**Artifact 로 올려 링크으로 확인합니다.** 사용자가 늘 여는 것은 진입 화면 하나뿐이고,
거기서 도구 세 개로 갑니다.

| 무엇 | 올리는 파일 | URL |
|---|---|---|
| 진행 상황 쪽지 (사용자가 여는 링크) | `preview/index.html` | https://claude.ai/code/artifact/f2a94114-3606-4a66-a75c-655f049fecd8 |
| **실제 진입 화면** | `preview/진입화면_한파일.html` | https://claude.ai/code/artifact/df4be102-cc11-4bbe-b160-7270e50d9826 |
| ① 문자생성기 | `dist/화학사고_주민대피문자_생성기.html` | https://claude.ai/code/artifact/a42de0d3-9c73-4d67-b048-b4bf9c9c2c53 |
| ② 주민 대피장소 찾기 | `dist/화학사고_주민대피장소_찾기.html` | https://claude.ai/code/artifact/c10d00e5-8ad9-454e-9caf-b4f010f00ef8 |
| ③ 방제 물품·장비 찾기 | `dist/화학사고_방제물품장비_찾기.html` | https://claude.ai/code/artifact/26c97527-e8ce-4629-8c12-0790afdceb87 |
| 디자인 방향 3안 (고르는 중) | `preview/design.html` | https://claude.ai/code/artifact/270fca8b-6a22-47b3-8dd1-58c66f0216bf |
| 방제자원 원자료 정리 (검토용) | `docs/방제자원_정리/한눈에보기.html` | https://claude.ai/code/artifact/a4d7613d-8a20-4384-966e-3442d5edf5f3 |
| **방제자원 통합 — 업체 섭외 · 방제물품** (검토용) | `docs/방제자원_정리/통합/한눈에보기.html` | https://claude.ai/code/artifact/3c2df1b6-1295-4d80-9c2e-e8e42bf6bbdf |
| **원내 자료 공부 + 최종 방향(안)** (검토용) | `preview/최종방향.html` | https://claude.ai/code/artifact/cf7fb0fa-5c7f-46ea-a3f5-429ddf0ce654 |
| **공유대회 [붙임2] 대표사례 작성본** | `preview/공유대회_붙임2.html` | https://claude.ai/code/artifact/a3905cc4-cfd3-4725-98da-d33965a20788 |

진입 화면은 `index.html` + `portal.css` + 로고로 되어 있어 Artifact(한 파일만
올라감)로 그대로 못 올립니다. `build_single.py` 가 한 덩어리로 합친
`preview/진입화면_한파일.html` 을 만들어 주며, 도구로 가는 링크는 위 표의 Artifact
주소로 바뀝니다(`PORTAL_LINKS`). **링크를 새로 올리면 그 표도 함께 고치세요.**

**화면이나 자료를 고쳤으면** `build_single.py` 로 `dist/` 를 다시 만든 뒤 그 파일들을
같은 URL 로 다시 올리세요. **다른 대화에서 올릴 때는 `url` 을 반드시 넘겨야** 같은
링크가 갱신됩니다 — 안 넘기면 링크가 새로 생겨 사용자가 보던 주소가 옛것이 됩니다.
`preview/index.html` 의 숫자·상태·남은 일도 같이 손봐야 실제와 맞습니다.

`preview/` 는 확인용이라 배포에 들어가지 않습니다(`make_release.py` 의 `SITE_ITEMS`
에 없음). 실제 진입 화면은 저장소 뿌리의 `index.html` 입니다.

Artifact 는 인터넷 주소를 막습니다(구글 폰트만 예외). 그래서 그 링크에서는
**배경지도 타일·주소검색·도보경로가 나오지 않습니다** — 고장이 아니고, 확인용
사본이 단일 파일판이라 원래 빠지는 기능입니다. 진입 화면에 그렇게 적어 두었습니다.

---

## 6. 이 개발 환경의 제약 — 바깥으로 못 나갑니다

게이트웨이가 대부분의 바깥 주소를 막습니다. 확인된 것:
`api.vworld.kr`(주소검색) · `api.odcloud.kr`(공공데이터포털) · `www.data.go.kr` ·
`www.safetydata.go.kr`(재난안전데이터공유플랫폼) · `safemap.go.kr` ·
nominatim · kakao · 그 밖의 지오코딩 서비스 전부.
`curl` 도 `WebFetch` 도 브라우저 `fetch` 도 똑같이 막힙니다. **우회할 방법이 없습니다.**

그래서 **바깥 자료가 필요한 일은 "브라우저에서 한 번 돌리는 페이지"로 만들어**
사용자에게 넘깁니다. 인터넷이 되는 PC에서 열면 자료를 받아 파일로 내려받고,
그 파일만 `data/` 에 넣는 방식입니다.

| 페이지 | 만드는 파일 | 무엇 |
|---|---|---|
| `build/fetch_tempshelter.html` | `data/tempshelters.js` | 이재민 임시주거시설 (행안부 오픈API · `safetydata.go.kr`) |
| `build/geocode.html` | `data/resources.geo.js` · `data/resources2.geo.js` | 방제자원 정확 좌표 (브이월드) |

**★ 깃허브 액션으로 받는 길도 있습니다(2026-09-07).** 액션 러너는 인터넷이
되므로 `build/fetch_tempshelter.py` 를
`.github/workflows/임시주거시설_받기.yml` 이 돌립니다(Actions → Run workflow).
사용자가 휴대전화만 들고 있을 때 저장소의 HTML 파일을 열 수 없어 만든 길입니다.
**⚠ 그런데 그 오픈API 는 인증키에 등록된 IP 에서만 받아집니다** — 아래를 보세요.

자료를 **파일로 내려받아** 건네받을 수도 있습니다. 그때는
`python3 build/make_tempshelters.py <내려받은.csv|xlsx|json>` — 위 페이지와 같은 규칙으로
같은 결과를 만듭니다. 사용자가 파일을 붙여 줄 수 있으면 이 길이 더 확실합니다.
**오픈API 응답을 그대로 저장한 `.json` 도 읽습니다**(2026-09-07) — 그 길이면
**칸 이름을 제가 직접 보고** 맞출 수 있어, 짐작으로 짝지은 것을 확인할 수 있습니다.

둘 다 **있으면 쓰고 없으면 그 기능만 빠지는** 선택 파일입니다
(`build_single.py` 의 `OPTIONAL`, `make_release.py` 의 `OPTIONAL`).

`build/fetch_tempshelter.html` 에는 **사용자의 인증키가 적혀 있습니다.**
사용자가 "열면 바로 돌게 해 달라"고 해서 넣은 것입니다. 그 파일은 `build/` 에 있어
배포 폴더·단일 파일에는 들어가지 않습니다.

**★ 그 페이지는 두 곳을 다 받습니다(2026-09-07).** 사용자가 재난안전데이터공유
플랫폼(`safetydata.go.kr`) 인증키를 받아 왔고, 기본 주소를
`https://www.safetydata.go.kr/V2/api/DSSP-IF-10945` 로 바꿨습니다.
- 쪽 넘기는 이름과 줄이 담긴 자리가 두 곳이 다릅니다 — 주소를 보고 가릅니다
  (`isSafety()`). safetydata 는 `serviceKey·pageNo·numOfRows` 에 줄이 `body`,
  공공데이터포털은 `page·perPage` 에 줄이 `data` 입니다.
- **★ 칸 이름을 확인했습니다(2026-09-08).** 사용자가 IP 를 등록하고 실제 응답
  (**총 15,911건**)을 보내 주었습니다. 확인된 칸 —
  `VT_ACMDFCLTY_NM`(시설명) · `DTL_ADRES`(주소) · `LA`/`LO`(좌표) ·
  `VT_ACMD_PSBL_NMPR`(수용인원) · `FCLTY_AR`(면적) · `KOREAN_CTPRVN_NM`(시도).
  **짐작으로 둔 규칙 두 개가 틀려 있었습니다** — 수용인원(`ACPT_PSN` 으로
  짐작 → 실제 `NMPR`)이 통째로 비었고, 시도를 `^CTPRVN$` 로 못 박아 못 잡았습니다.
  둘 다 고쳤고 `tests/fetchpage` 가 실제 칸 이름으로 검사합니다.
  · **이 자료에 관리기관·전화가 아예 없습니다.** 비웁니다 — 지어내면 담당자가
    틀린 곳으로 전화를 겁니다. ② 목록의 그 줄은 비어 나옵니다(고장 아님).
  · **시설구분은 뜻이 적히지 않은 코드**(`ACMDFCLTY_SE_CD` = 001·003·018 …)
    뿐이라 비웁니다. **코드표를 받으면** 그때 채웁니다.
  · **`RN_DTL_ADRES` 를 주소로 쓰지 마세요** — 이름은 도로명주소 같은데 값이
    **"경로당"·"주민공동시설-1"** 같은 **건물 안 자리 이름**입니다.
    `SGG_RN` 도 번지 없는 **길 이름만** 있습니다. 둘 다 `NEVER` 로 막았습니다.
  · **시·군·구 칸이 없습니다** — 주소에서 뽑습니다. 1,000줄로 대조해 보니
    대피장소 자료에 없는 이름이 **하나도 없었습니다**(세종은 `null` 열쇠).
  · 화면은 여전히 그 짝과 **원자료 첫 줄을 그대로** 띄웁니다
    (`원자료 첫 줄 복사`) — 자료가 바뀌면 그 한 줄로 고칩니다.
- **★ 공식 명세로 한 번 더 대조했습니다(2026-09-08 · 오픈API 이용가이드).**
  15개 칸이 우리 짝짓기와 **정확히 같습니다** —
  `VT_ACMD_PSBL_NMPR` = **이재민수용가능인원**, `FCLTY_AR` = 시설면적,
  `KOREAN_CTPRVN_NM` = 한글시도명, `DTL_ADRES` = 상세주소.
  · ⚠ **`RN_DTL_ADRES` 의 공식 이름은 "도로명상세주소" 입니다** — 이름만 보면
    주소인데 **실제 값은 "경로당"·"주민공동시설-1"** 입니다(1,000줄 확인).
    **명세 이름보다 실제 값이 우선입니다.** `NEVER` 로 막은 것을 풀지 마세요.
  · ⚠ **시설구분 코드표는 이용가이드에도 없습니다** — `ACMDFCLTY_SE_CD` 의
    설명이 "수용시설구분코드" 로 이름만 되풀이하고 첨부파일도 없습니다.
    관측된 값은 `001·002·003·007·015·016·017·018·019` 입니다.
    **뜻을 모르므로 비웁니다** — 받으면 그때 채웁니다.
  · **갱신주기가 `1일` 입니다**(갱신일자 2026-09-08). 예전에 페이지에
    "보통 1년에 한 번" 이라고 적어 두었던 것은 **제 짐작이었고 틀렸습니다.**
    받은 파일은 **받은 날의 사본**이라 화면의 **자료 기준일**이 더 중요합니다.
  · 요청 변수에 **`startLat`·`endLat`·`startLot`·`endLot`**(위경도 범위)가
    있습니다. 지금은 전국을 통째로 받으므로 안 쓰지만, 나중에 지역만
    받아야 할 때 쓸 수 있습니다.
- **★ 한 번에 1,000줄까지만 옵니다.** `numOfRows=99999` 를 넣어도 서버가
  **1000 으로 깎아서** 돌려줍니다(확인함). 15,911건이면 **16쪽**입니다.
  · 페이지의 `perPage` 를 100 → **1000** 으로 올렸습니다(160번 → 16번).
    하루 한도가 1,000번이라 160번은 한 번 실수하면 그날 못 받습니다.
  · 주소창으로 저장하는 길은 파일이 16개가 되므로, 파일 고르기를
    **여러 개(`multiple`)** 로 바꾸고 이어 붙입니다. 겹쳐 저장한 줄은
    원자료 그대로를 열쇠로 걸러냅니다(`tests/fetchpage` 가 검사).
- **★ 페이지가 저장소 밖에서도 정확히 돕니다.** 사용자는 이 HTML 한 장만
  내려받아 다운로드 폴더에서 엽니다 — 그러면 옆의 `../data/shelters.js` 를
  못 읽어 지역 이름이 어긋납니다(특히 **세종**: 대피장소 자료는 시·군·구
  열쇠가 `null` 인데 "세종특별자치시" 로 들어가 두 층이 따로 놉니다).
  그래서 **시·도/시·군·구 이름표만**(1.5KB · 좌표 없음) 페이지에 박아 두고,
  `data/shelters.js` 가 없을 때만 씁니다(`REGION_FALLBACK`).
  대피장소 자료의 시·군·구가 바뀌면 **그 줄도 함께 고치세요**(손 갱신).
- **기억하는 이름(localStorage)을 예전과 다르게 두었습니다** — 예전에 그
  페이지를 열어 본 브라우저에는 **쓸 수 없는 집계표 주소**가 저장되어 있어,
  같은 이름을 쓰면 새 주소를 적어 두고도 옛 주소로 받아 옵니다.
- **★ 실제로 불러 봤습니다(2026-09-07 · 깃허브 액션). 서버가 이렇게
  답했습니다 — `32 UNREGISTERED IP ERROR`.**
  네트워크는 뚫렸고 인증키도 살아 있는데, **이 오픈API 는 활용신청 때 적어 둔
  IP 에서만** 받아집니다. 액션 러너 IP 는 돌 때마다 달라 등록해 둘 수 없습니다.
  · 그래서 **사용자가 한 번 해 주어야 하는 일이 남습니다** —
    `safetydata.go.kr` 로그인 → 마이페이지 → 오픈API 활용신청 현황 →
    그 API 의 **활용 IP(서버 IP)** 에 부를 자리의 IP 를 넣기(제한 없음이
    가능하면 그렇게). 그 자리에서 `build/fetch_tempshelter.html` 을 열면 됩니다.
  · **또는 플랫폼에서 목록을 파일로 내려받아** 넘겨 주면
    `python3 build/make_tempshelters.py 받은것.json` 으로 같은 결과가 됩니다.
  · 이 오류를 **세 곳이 모두 사람 말로 풀어 줍니다**(브라우저 페이지 ·
    파이썬 · 워크플로 로그). `tests/fetchpage` 가 그 문구를 검사합니다 —
    "32" 만 띄우면 무엇을 해야 하는지 아는 사람이 없습니다.
- **★ `data.go.kr` 은 액션 러너에서도 안 됩니다 — 아예 시간초과입니다**
  (2026-09-07 · 후보 셋 모두). 러너가 미국(Azure)에 있어 막히는 것으로
  보입니다(그날 러너 IP `172.215.210.49`). **그러니 CI 로 받아 보려고
  다시 시도하지 마세요** — 두 번 확인했습니다.
  → **국내 IP 에서 한 번 받는 것**이 유일한 길입니다. 사용자에게 건네는
    2분짜리 안내는 **`docs/임시주거시설_받는법.md`** 에 있습니다.
- **아직 `data/tempshelters.js` 가 없습니다.** 그 파일이 생기기 전까지
  ② 지도에 그 층은 **나오지 않습니다** — 코드는 다 되어 있고 자료만 없는
  상태입니다(고장이 아닙니다).
- **★ 자료가 들어와도 지도가 버티는지는 미리 재 두었습니다** —
  `tests/run.sh tmpbig` 이 **만 곳(10,992)** 을 넣고 ② 를 실제로 눌러 봅니다
  (여는 데 5.0초 · 시·군·구 고르기 1.6초 · 반경 20km 137곳 2.2초 · 끌기 0.9초).
  전국 이재민 임시주거시설은 만 곳 단위라, 자료가 온 날 지도가 멈추면 그때는
  사고가 난 뒤일 수 있습니다. 그래서 **자료가 오기 전에** 재 둡니다.
  그 검사는 자료 파일을 만들지 않고 `addInitScript` 로 넣습니다 — `data/` 를
  건드리면 다른 묶음과 섞입니다.

---

## 7. 이미 정한 것 — 되돌리지 마세요

사용자와 합의했거나, 한 번 해 봤다가 되돌린 것들입니다.

- **이재민 임시주거시설은 화학사고 대피장소와 "별개의 층"으로만** 다룹니다.
  예전에 "실제 대피는 이재민 임시주거시설로"라는 문구를 목록 위에 넣었다가
  사용자가 명확히 거부했습니다(`580fdee` 에서 되돌림). 자료를 나란히 보여 주되
  **어느 쪽이 진짜 대피처라는 식의 단정은 넣지 않습니다.**
- **방제자원에 개인 휴대전화번호를 넣지 않습니다.** 사업장 대표번호만.
  확인 못 한 번호는 기관 이름만 적고 번호는 비웁니다.
  (`make_resources.py` 가 `01`로 시작하는 번호를 발견하면 빌드를 멈춥니다)
- **① 도로 우회 알림은 `실내 대피`·`대피명령` 두 분류**로 나뉘고, 각 분류의
  상황종료 문안은 따로 접었다 폅니다. 분류 딱지는 발송문안 위에 작게 둡니다.
- **★ ③ 은 이름이 `방제 물품·장비 찾기` 이고 시작이 세 걸음입니다
  (2026-09-07 · 사용자 분류안 PPT).**
  `걸음 1 무엇을 하려고 하십니까?(두 갈래) → 걸음 2 무엇이 필요하세요? →
  걸음 3 사고지점이 어디인가요? → 결과`.
  사용자 요구입니다 — **"결국 방제자원은 맨처음 2가지 큰 틀로 나뉘어질거야.
  업체 섭외와 방제물품 찾기 두가지로."**
  - **업체 섭외** — 폐기물·폐수를 맡길 곳, 중장비·독성가스 회수를 부를 곳.
    걸음 2에서 **허가 갈래 7가지**를 고릅니다(`RES2_CATS`).
  - **방제물품 찾기** — 흡착포·보호복·굴착기 같은 **물건**. 걸음 2에서
    **표준 이름 27가지**를 고릅니다(`RES2_NAMES`). 고른 물품을 가진 곳만
    나오고, 목록 줄에도 **고른 물품만** 적습니다(사용자 — "사용자가 딱
    원하는 물품만 확인 할 수 있도록").
  - **왜 맨 처음에 갈라 묻나** — 두 갈래는 묻는 것이 다릅니다("어디에
    맡길까" ↔ "무엇이 어디 있나"). 한 화면에 섞으면 지금 무엇을 고르는
    중인지 알 수 없습니다.
  - 걸음 2의 값은 **자료에서 그대로** 옵니다 — 지어낸 분류가 아닙니다.
    칸에 적는 건수도 세어서 적습니다(`tests/res2` 가 자료와 대조).
  - **하나도 안 고르면 다음으로 못 갑니다** — "고른 것"이 거짓이 됩니다.
    대신 **"무엇이 필요한지 모르겠어요 — 전부 보기"** 를 옆에 둡니다.
    모르는 사람을 막다른 길에 세우지 않습니다.
  - **되돌아가도 고른 것은 남습니다.** 사고지점만 잘못 찍어 되돌아오는 경우가
    대부분인데 고른 것까지 지우면 처음부터 다시 해야 합니다.
  - **⚠ 예전 흐름(걸음 1 = 필요한 것 7갈래, 자료 470건)은 이것으로
    대체되었습니다.** 그 7갈래는 사용자가 정한 것이었지만, 분류안 PPT 가
    더 나중의 결정입니다. **되돌리려면 사용자에게 먼저 물어야 합니다.**
  - ③ 이 읽는 자료가 `data/resources.js`(470건) → **`data/resources2.js`**
    (업체 1,717줄 · 물품 4,075줄 · 자리 2,364곳)로 바뀌었습니다.
    `build/make_resources2.py` 가 `docs/방제자원_정리/통합` 에서 만듭니다.
    · **자리(`RES2_PLACE`)를 따로 둡니다** — 물품 4,075줄이 실제로는 한 곳에
      여러 물품이라, 줄마다 주소를 되풀이하면 파일이 세 배가 되고 지도에도
      같은 자리에 마커가 여러 개 찍힙니다.
    · **지도 마커의 종류가 갈래마다 다릅니다** — 업체는 **허가 갈래**(무엇을
      맡길 수 있는 곳인가), 물품은 **보유처 종류**(누가 가지고 있는가).
      물품에서 종류를 물품 이름으로 두면 한 시청이 여러 색이 됩니다.
    · 예전 자료(`resources.js`)는 `make_resources.py` 가 그대로 만들지만
      **화면이 부르지 않습니다.** `build/geocode.html` 만 아직 씁니다.
    · **내부판 단일 파일(`--internal`)은 없어졌습니다** — 새 자료에는 담당자
      개인 연락처를 처음부터 넣지 않아 '내부용'으로 더 담을 것이 없습니다.
- **③ 의 갈래·보유처·물품 분류에는 각각 아이콘이 있습니다**(`res/app.js`
  의 `ICONS`). 사용자 요구 — "아이콘만 보면 이게 무엇이겠구나 유추가 될만한"
  것. 걸음 1·2 카드 · 범례 · 목록 줄 · 조건 칩에 같은 그림을 씁니다.
  두 갈래로 다시 짜면서 여섯 가지를 더 그렸습니다 — 수화기(업체 섭외) ·
  굴뚝(중간처분) · 재활용 화살표 · 아래 화살표+땅(매립) · 굴착기(중장비) ·
  스패너(작업도구) · 사람 둘(인력).
  - **그림만으로 뜻을 전하지 않습니다.** 옆에는 늘 이름이 있고 그림은
    `aria-hidden` 입니다. 그림이 안 그려져도 읽는 데 지장이 없어야 합니다.
  - **★ 지도 마커에도 같은 그림이 들어갑니다**(2026-09-07). 예전에는 지도가
    **색 동그라미뿐**이어서 "이 색이 저 종류"를 색으로만 외워야 했습니다
    (사용자 지적 — "각각 아이콘을 실컷 설정해놨는데 왜 지도에는 반영이
    안되고 색깔 점으로 나오는거야?"). `MAPCORE.pinIcon()` 이 **채운 원 +
    그림**을 그리고 ②③ 이 함께 씁니다.
    · 그림은 `--on-deep`(밝은 선)으로 얹습니다 — **흰색을 직접 적지
      않습니다**(고대비 화면에서도 같은 대비가 나오는 토큰).
    · **어림잡은 좌표(`approx`)는 속을 비우고 그림도 종류 색**으로 바꿉니다.
      빈 원 위에서 밝은 선은 보이지 않습니다.
    · 그림에 `pointer-events:none` — 누를 곳을 찾는 판정은 **좌표로**
      합니다(가장 가까운 마커). 그림이 이벤트를 먹으면 안 됩니다.
    · 지름이 14px 아래로 내려가면 **그림을 아예 그리지 않습니다** — 뭉개져서
      점과 구별이 안 되는데 그리는 값은 그대로 듭니다.
    · `tests/res2`·`tests/n1` 이 검사합니다 — 옛 `circle.rk`/`circle.mk` 가
      남아 있으면 실패하고, 범례 그림 색과 마커 색을 대조합니다.
  - 뜻이 같은 것끼리는 **같은 그림**을 씁니다(종류 '독성가스 회수업체' 와
    필요한 것 '가스 회수'). 다르게 그리면 다른 것으로 읽힙니다.
- **① 문자 도구는 한 걸음에 한 묶음만 묻습니다**(단계형). 입력 8칸을 한 화면에
  쌓아 두지 않습니다. 그리고 **필수 칸이 비면 문안을 만들지 않습니다** — 반쪽
  문안을 그대로 복사해 발송할 위험이 있습니다.
- **집결지는 비워도 됩니다.** 집결지를 두지 않는 지자체가 있어 필수에서 뺐습니다.
  비면 7번 문안이 `[[!집결지:…]]` 쪽 문장으로 나갑니다(대피소만 넣은 문장).
  **기계가 문장을 잘라 붙이는 것이 아니라 미리 적어 둔 두 문장 중 하나가 나갑니다** —
  문안은 승인받은 표준(안)이라 조립해 만들면 안 됩니다.
- **사고지점·내 위치·위도·경도는 조건 줄(위쪽)에 둡니다.** 예전에는 지도 아래
  반투명 띠에 있었는데 지도에 가려 못 찾았습니다(사용자 요청으로 옮김).
  ②③ 둘 다 같은 자리입니다 — 두 지도가 달라 보이면 안 됩니다.
- **어림잡은 위치를 사고지점으로 찍을 때는 무엇으로 어림잡았는지 적습니다.**
  ① 의 '지도에서 찾기'는 시·군·구·읍·면·동으로 자리를 잡아 여는데
  (`MAPCORE.placeAt`), 그때 창 위에 붉은 줄로 근거와 "다시 찍으라"는 안내를
  띄웁니다(`.shmap-guess`). 몇백 미터 틀릴 수 있는 값입니다.
- **진입 화면의 도구 차례와 이름은 사용자가 정합니다.** 지금은
  `01 방제 물품·장비 찾기 → 02 주민 대피장소 찾기 → 03 주민대피 문자생성기`.
  - 한동안 `01 대피장소 지도 → 02 주민대피 문자 작성 → 03 방제 동원 체계`
    였습니다. 상사 피드백으로 정한 '실제 업무 순서'라고 적어 두었는데
    (`b0802d4`), 사용자가 지금 차례로 바꾸라고 했습니다.
    **임의로 되돌리지 마세요** — 되돌리려면 사용자에게 먼저 물어야 합니다.
  - 카드 번호(01·02·03)는 화면의 차례이므로 CLAUDE.md 1절의 ①②③(폴더
    이름표)과 어긋나는 것이 맞습니다.
  - **이름도 사용자가 정한 것입니다** — `방제 동원 체계 → 방제 물품·장비 찾기`,
    `대피장소 지도 → 주민 대피장소 찾기`, `주민대피 문자 작성 → 주민대피
    문자생성기`. "더 직관적으로" 라는 요구였습니다. 화면 제목·카드·단일 파일
    이름·배포 안내가 모두 같은 이름이어야 합니다.
  - **머리띠 문구 둘째~넷째 줄도 카드와 같은 차례**입니다(`tests/t9` 가 카드
    링크 차례를 검사합니다).
- **카드 색은 세 장 모두 같은 파랑(Primary)입니다.** 예전에는 도구마다 다른
  원색(초록·파랑·청록)이었는데, 사용자가 그려 온 개선(안)의 배지가 셋 다
  파랑이고 KRDS 도 "색은 역할로 쓴다 — 기능마다 다른 원색을 칠하지 않는다"고
  하므로 통일했습니다. 빨강은 사고지점처럼 **실제 위험**에만, 초록은 지도
  그림의 **대피장소 마커**에만 남겼습니다(도구 화면의 마커 색과 같게).
- **⚠ 머리띠 사진이 첫 화면을 가득 채우고, 카드 세 장은 그 위에 뜹니다
  (2026-09-07).** 사용자 요청입니다 — "이 사진이 화면을 가득 채우고 우리의
  3가지 도구 버튼은 지금 자리 그대로 가득 찬 화면 위에 있으면 좋겠어."
  - `index.html` 의 **머리띠와 `<main>` 이 `.cover` 한 덩어리** 안에 있고,
    사진(`.hero-ph`)은 `.hero` 밖 · `.cover` 바로 아래에 있습니다.
    `.hero` 안에 두면 카드 자리까지 덮을 수 없습니다.
  - **사진 칸 높이를 px 로 못 박지 마세요.** `.cover` 가 격자(2줄:
    머리띠 / 도구 띠)이고 사진은 칸을 골라 덮습니다 — 좁은 화면은 첫째 줄만
    (`grid-area:1/1`), 넓은 화면은 두 줄 전부(`grid-row:1/-1`). "머리띠
    높이만큼 아래로" 를 숫자로 적으면 글자 길이·글꼴·화면 폭이 조금만 달라져도
    사진이 잘리거나 회색 띠가 드러납니다.
  - 화면 높이도 계산하지 않습니다 — `body` 가 세로 flex 이고 `.cover` 가
    `flex:1 0 auto` 라 **상단 바 아래부터 꼬리 위까지** 저절로 찹니다.
  - **글자 자리와 막(그라데이션)은 그대로 둡니다** — 사용자 조건입니다.
    막(`.hero-veil`)은 사진과 같은 칸을 덮으므로 넓은 화면에서는 카드 아래까지
    이어집니다. 머리띠에서만 끊으면 사진 위에 **가로줄 하나**가 남습니다.
- **⚠ 넓은 화면(≥800px)에서는 카드마다 테두리·둥근 모서리·틈이 있습니다
  (2026-09-07).** 사용자 요청 — "가운데 세가지 박스는 전체화면 위에 위치하는
  만큼 각 박스별로 확실한 태두리를 가져야 해."
  - 한동안은 반대였습니다. 카드마다 테두리를 두고 간격을 벌렸더니 **"박스
    사이의 공백이 오히려 전문성을 줄인다"** 고 해서 붙였었습니다. 그때는
    밑바탕이 회색이라 붙여야 한 묶음으로 읽혔는데, 이제 **사진 위에 떠 있어**
    붙여 두면 흰 판 하나로 보여 셋인 줄 모릅니다. 그 지시는 대체되었습니다.
  - **좁은 화면은 예전 그대로 붙인 한 덩어리**입니다 — 세 칸이 한 화면에
    들어와야 해서 사이를 벌릴 여유가 없습니다(`tests/t9` 16).
  - 테두리 색은 `--pn-line` 입니다(밝음 gray-30 · 고대비 gray-40).
    칸막이(`--line`)보다 한 단 진합니다 — 사진 위에서 옅은 선은 사진 무늬에
    묻힙니다. 고대비는 카드 바탕이 검정이라 **밝은 쪽**으로 잡습니다.
  - 초점 표시는 안쪽으로 그립니다(`--focus-in`) — 좁은 화면에서는 카드끼리
    붙어 있어 밖으로 그리면 옆 카드 위로 넘어갑니다.
- **⚠ 가운데 상자는 `clamp(120rem, 88vw, 160rem)` 입니다 — 세 단계를 거친
  값이니 임의로 되돌리지 마세요.**
  - ① 상자를 없애고 화면 끝까지 늘렸다가 **"화면 비율이 완전히 바뀌어 오히려
    가독성이 떨어졌다"** 고 되돌렸습니다. 실제 문제는 **한 칸이 620px 을
    넘은 것** — 사진이 커지고 글자가 흩어집니다.
  - ② 그래서 120rem(1200px)으로 못 박았는데, **사용자가 실제로 쓰는 화면이
    2330px** 이라 띠가 화면의 49% 밖에 안 되고 **양쪽에 589px 씩 공백**이
    생겼습니다(2026-09-07 지적). 제가 확인용으로 늘 1440px 에서만 찍어 보고
    있어서 못 보고 있었습니다 — 거기서는 80% 를 채웁니다.
    **넓은 화면(1863·2330px)에서도 재 보세요.**
  - ③ 사용자가 **1600px 안**을 골랐습니다. 2330px 에서 양쪽 389px 씩 ·
    띠가 67% · 한 칸 507px — ① 에서 문제였던 620px 보다 충분히 작습니다.
  - **`160rem` 하나로 못 박으면 안 됩니다** — 1440px 화면에서 상자가 화면
    끝(여백 24px)에 닿아 ① 로 돌아갑니다. `88vw` 를 끼워 **화면의 12% 는
    언제나 여백**으로 남깁니다. 1440px → 1320px · 1863px 이상 → 1600px.
  - `tests/t9` 가 세 가지를 함께 잽니다: 띠 ≤ 1560px · 화면 끝에서 40px
    이상 · 한 칸 < 620px.
  - **사진만 화면을 채우고 카드는 이 상자 안에 있습니다** — 위 항목과
    어긋나지 않습니다.
- **로고·머리띠 제목·카드 띠·푸터의 왼쪽 끝은 한 줄(`--edge`)에 섭니다.**
  하나만 다르게 두면 그 줄만 밀려 어긋나 보입니다(`tests/t9` 가 검사).
- **카드 띠는 머리띠 글 바로 아래에 붙습니다.** 사이에 '업무도구 3종' 제목이
  있어 틈이 생겼는데, 사용자가 "그 갭 없이 딱 붙어 있으면 좋겠다"고 했습니다.
  제목은 화면에서 빼고 문서에는 남겼습니다(`.sr-only` — 화면낭독기와 제목
  계단 h1→h2→h3 에 필요). `tests/t9` 17 이 그 틈을 1px 이하로 잽니다 —
  사진이 화면을 채우게 된 뒤에도 **카드 자리는 그대로**입니다.
- **카드에 평소 보이는 글자는 `01 이름` 한 줄뿐입니다.** 자료 건수·기준일은
  설명과 함께 사진 위 판(`.pn-ov`)으로 옮겼습니다 — 사용자가 "그 문구가 굳이
  나타나 있지 않아도 될 것 같다. 마우스를 올리면 설명 정도만" 이라고 했습니다.
  손가락으로 쓰는 기기에서는 판이 늘 펼쳐져 있으므로 자료 건수만 화면에서
  비켜 둡니다(sr-only 방식 — 화면낭독기는 그대로 읽습니다).
- **⚠ '업무 참고도구입니다 …' 안내는 진입 화면에서 걷어냈습니다(2026-09-04).**
  사용자 결정입니다 — "이 멘트 싹 지우기로 결정이 되었어. 다 지워버리고
  우리가 필요한 화면만 남기자." **임의로 되살리지 마세요** — 되살리려면
  사용자에게 먼저 물어야 합니다.
  - 한동안은 반대였습니다. 노란 띠 + 왼쪽 굵은 선이던 것을 "너무 크게 눈에
    잘 띈다. 명시는 하되 눈에 잘 안 띄도록" 이라고 해서 작은 회색 글줄로
    눌러 두고 **"지우지는 마세요"** 라고 적어 두었었습니다. 그 지시는
    이제 위 결정으로 대체되었습니다.
  - **같은 내용이 도구 안에는 그대로 있습니다** — ②(`map/index.html`)와
    ③(`res/index.html`)의 참고도구 알림. 걷어낸 것은 **진입 화면 한 곳**
    뿐입니다.
  - `tests/t9` 17 이 이제 **반대를 검사합니다** — `.notice` 요소도, '업무
    참고도구' 라는 글자도 진입 화면에 없어야 통과합니다.
- **좁은 화면의 도구 이름은 유리 띠 위에 24px 로 얹습니다.** 사진 위에 얹힌
  이름이 19px 이라 **"글씨가 너무 작다"** 는 지적을 받았습니다(2026-09-04).
  사진 위에서 글자만 키우면 사진 밝기에 따라 오히려 읽기 나빠지므로,
  글자가 앉는 자리에 **뒤를 흐리는 반투명 띠**(`--band` +
  `backdrop-filter`)를 깔고 그 위에 얹었습니다.
  - **띠는 사진 칸 안쪽에 앉습니다** — 카드 높이는 153px 그대로라 셋째
    칸이 화면 밖으로 밀리지 않습니다(`tests/t9` 16 이 두 가지를 같은
    자리에서 잽니다: 이름 22px 이상 · 한 줄 · 칸 175px 이하).
  - 번호(`01`)는 이름이 커진 만큼 **채운 배지 → 테두리만 남긴 알약**으로
    낮췄습니다. 번호가 이름보다 세면 안 됩니다(KRDS).
  - **흐리기를 모르는 브라우저에서도 읽혀야 합니다** — `backdrop-filter`
    가 없으면 반투명 배경만 남으므로 `--band` 를 그 상태로도 흰 글자가
    읽힐 만큼 짙게 잡았습니다(밝음 .70 · 고대비 .86).
  - `--band`·`--band-on`·`--band-line` 은 **밝은 `:root` 와 고대비 블록
    양쪽에** 있습니다(3절의 규칙 — 한쪽만 넣으면 다른 모드에서 그 줄이
    통째로 버려집니다).
- **서비스 이름은 「화학사고 초동대응 지원 서비스」입니다.** 사용자가 정해
  알려 준 공식 이름입니다(전에는 "화학사고 대응지원 서비스"였습니다).
  적히는 곳은 진입 화면의 탭 제목·로고 옆 이름(`.svc`)·머리띠 큰 제목,
  그리고 `make_release.py` 의 `SITE_NAME`(배포 폴더·zip 이름)과 전달 안내문
  입니다. **마음대로 줄이거나 고쳐 쓰지 마세요.**
- **머리띠 제목은 낱말로 나뉘어 하나씩 올라옵니다.** "제목 문구가 너무 정적"
  이라는 지적에 대한 답입니다. 서비스 이름은 기관의 공식 이름이라 **글자는
  바꾸지 않고** 들어오는 방식만 살렸습니다. 지어낸 홍보 문구를 넣지 마세요.
  낱말 수가 바뀌면 `portal.css` 의 `.hero h1 span:nth-child(N)` 지연을 그만큼
  더해야 합니다 — 없으면 그 낱말만 지연 없이 먼저 떠오릅니다(`tests/t9` 가
  낱말 수와 지연을 함께 셉니다).
- **머리띠는 사진 다섯 장과 문구 다섯 줄이 5초마다 함께 바뀝니다**(한 바퀴
  25초). 사용자 요청입니다 — "긴 배경화면 사진이 5초쯤마다 바뀌면서 문구도
  다른 문구로 바뀌었으면 좋겠다". 이 네 가지가 함께 있어야 합니다.
  - **넘김은 CSS 만으로** 합니다(`heroShot`·`heroLead`·`heroDot`). 자바스크립트가
    꺼져도, 망분리 PC 의 단일 파일에서도 그대로 넘어가야 합니다.
  - **안 보이는 문구 네 줄도 문서에 그대로** 있습니다(`opacity` 만 0).
    화면낭독기는 다섯 줄을 다 읽습니다 — 움직임 뒤에 자료를 숨기지 않습니다.
  - **멈춤 단추**가 있어야 합니다(WCAG 2.2.2 — 5초를 넘겨 저절로 움직이는
    것에는 멈출 방법을 준다). `assets/portal.js` 가 만들고, CSS 가
    `animation-play-state:paused` 로 **그 자리에 세웁니다**(애니메이션을
    지우면 첫 장으로 되돌아가 버립니다). 모션을 줄이는 설정에서는 넘김이
    이미 꺼져 있으므로 **단추를 만들지 않습니다.**
  - 다섯 줄은 **한 칸에 겹쳐** 둡니다(`.hero-leads` 가 grid). 세로로 쌓으면
    줄 수가 다른 문구로 넘어갈 때마다 띠 높이가 들썩입니다.
  **문구에 지어낸 수치를 넣지 마세요** — 네 줄에 적은 숫자(1,849곳 · 12건 ·
  42건 · 470건 · 6종)는 카드의 자료 건수와 **같은 값**이어야 하고 `tests/t9`
  가 두 값을 대조합니다. 다섯째 줄은 이 도구의 원칙("판단하지 않습니다")입니다.
  사진은 **비슷한 것을 나란히 두지 마세요** — 눈높이 파노라마와 수직 항공을
  번갈아 놓았습니다(`build/make_site_images.py` 의 `CROP` 차례). 닮은 두 장이
  잇달아 나오면 바뀐 줄 모르고 화면이 잘못된 것처럼 보입니다.
  띠가 40px 높아지면 휴대전화에서 셋째 칸이 화면 밖으로 밀립니다 — 그래서
  좁은 화면의 머리띠 여백을 함께 눌렀습니다(`tests/t9` 16 이 재고 있습니다).
- **좁은 화면에서는 카드 한 장이 사진 한 장입니다.** 사용자 요청 —
  "모바일에서는 설명은 지우고 제목만. 사진 위에 글을 쓰는 거야. 칸이 지금보다
  좀 더 좁아지는 거지." 흰 글자 칸을 없애고 제목을 사진 아래쪽에 얹었고,
  그 자리에 짙은 판(`--ov` 그라데이션)을 깔아 사진 밝기에 기대지 않게 했습니다.
  한 칸 153px — 실제 휴대전화(390×750)에서 **세 칸이 온전히** 들어옵니다.
  - 설명·자료 건수는 **화면에서만 비켜 둡니다**(sr-only 방식). 문서에서 지우면
    화면낭독기 사용자가 도구가 무엇을 하는지 알 수 없습니다.
  - 제목이 판 아래쪽에 앉으므로 사진마다 `object-position` 을 따로 잡아
    **주인공이 위쪽에** 오게 했습니다(지도는 사고지점 핀, 문자는 문안 윗줄).
  - **눌렀다는 느낌을 `:active` 로 줍니다** — 사진 1.05배 + 판이 짙어짐 +
    제목 줄이 오른쪽으로 밀림 + 화살표가 파랗게 참. 손가락에는 호버가 없으니
    이것이 유일한 눌림 신호입니다. iOS 사파리가 `:active` 를 켜도록
    `assets/portal.js` 가 아무 일도 하지 않는 `touchstart` 듣는 이를 하나
    달아 둡니다 — 지우면 iOS 에서 눌린 느낌이 사라집니다.
- **평소에는 단순한 카드, 마우스를 올리면 설명.** 사용자 요청입니다 —
  "굉장히 미니멀 하고 심플하지만, 마우스를 올리면 지금 페이지에 나와 있는
  설명이 보여지도록". 평소에는 그림 + `01 이름` + 자료 건수 + 둥근 화살표만
  보이고, 올리거나 초점이 오면 설명(`.pn-ov`)이 그림 위로 올라옵니다.
  **감추는 것이 아니라 비켜 두는 것입니다** — `opacity` 만 0 이라 화면낭독기는
  늘 읽고, 호버가 없는 기기(`@media (hover:none)`)와 인쇄에서는 처음부터
  펼쳐 둡니다. 이 세 가지가 함께 있어야 합니다. `display:none`·
  `visibility:hidden` 으로 지우면 접근성이 깨집니다(`tests/t9` 가 검사).
- **사진 위 딱지(`.pn-lb`)는 하나씩 나타났다 사라집니다.** "사진 위에 문자들이
  살아 움직이는 느낌"을 달라는 요청입니다. **딱지에 수치를 적지 않습니다** —
  거리·시간을 적으면 자료처럼 읽혀 잘못된 값을 주게 됩니다. 사진에 실제로
  찍혀 있는 것의 이름(대피소·사고지점·방제장비 …)만 씁니다
  (`tests/t9` 가 숫자를 검사).
- **움직이는 것은 이것뿐입니다** — 열 때 한 번: 제목·부제가 올라오고, 카드
  세 장이 차례로 올라옵니다. 그다음 되풀이되는 것: 머리띠 사진·문구가 5초마다
  바뀌고(위), 사진 위 딱지가 하나씩 나타났다 사라집니다.
  영상처럼 움직이는 것은 없습니다. **멈춘 상태가 곧 다 보이는 상태**라야 하고,
  모션을 줄이는 설정에서는 `animation:none` 으로 **끕니다**(길이만 0 으로
  줄이면 무한 반복이 순식간에 되풀이돼 오히려 깜빡입니다).
- **진입 화면은 실사 사진 8장으로 되어 있습니다** (머리띠 5 + 카드 3).
  사용자가 자리마다 맞는 사진을 직접 골라 주었습니다 — 머리띠는 산업단지·
  정유단지 다섯 장이 번갈아, 카드는 `항공 지도 + 영향반경` ·
  `재난문자를 확인하는 휴대전화` · `방제지원차량과 대응인력`.
  원본은 `source/photos/`(배포 제외)에 있습니다.
  - **왜 예전에는 사진을 뺐나** — 현장 사진 8장을 아무 자리에나 깔았다가
    "오히려 더 복잡해졌다"고 되돌렸습니다. 그때 문제는 *사진 자체*가 아니라
    **자리마다 무엇을 보여줄지가 없었던 것**입니다. 지금은 사진 하나가
    그 도구가 하는 일을 그대로 보여 줍니다. **아무 사진이나 다시 깔지 마세요.**
  - **'현장 사진' 같은 갤러리 칸은 두지 않습니다.** 한 번 만들었다가
    "사이트에서 보여줄 필요가 없다"는 말을 듣고 없앴습니다. 사진은 도구를
    설명하는 자리에만 들어갑니다.
- **사진은 `build/make_site_images.py` 로 미리 잘라 씁니다.** 세 카드의 비율이
  정확히 같아야(**16:9**) 카드 줄이 맞는데, 브라우저의 `object-fit` 에만 맡기면
  사진마다 잘리는 자리가 달라 **사고지점 핀이나 사람이 잘려 나갑니다.**
  자리마다 어디를 남길지 그 스크립트의 `CROP` 에 적혀 있습니다.
  사진을 새로 받으면 `source/photos/` 에 같은 이름으로 넣고 스크립트를 다시
  돌린 뒤, `portal.css` 의 딱지 자리(`.pn.tN .lN`)도 함께 맞추세요 — 안 그러면
  딱지가 엉뚱한 곳을 가리킵니다.
  **가로 픽셀을 보이는 크기에 맞춰 줄이지 마세요.** 요즘 화면은 같은 자리에
  화소를 2~3배로 그려서, 딱 맞춰 두면 늘어나 뿌옇게 보입니다. 머리띠를
  1500px 로 줄였다가 **사용자가 "사진이 깨진다"** 고 했습니다 — 1440px 화면에서
  2880 화소로 그려지고 있었습니다. 지금은 머리띠 `width=2400`(원본
  1672~2000px 보다 커서 사실상 자르기만 함) · 카드 `width=1200` 입니다.
  단일 파일이 1MB 쯤 커지지만 그쪽이 맞는 선택입니다.
  ⚠ **숫자만 올려서는 없는 화소가 생기지 않습니다.** 지금 또렷함의 한계는
  원본 사진 크기(1672~2000px)입니다. 더 좋게 하려면 **더 큰 원본**을
  받아야 합니다 — 사용자에게 물어보세요.
- **사진 위 글자는 막(`--veil`)을 깔고 얹습니다.** 사진은 자리마다 밝기가
  들쭉날쭉해서 글자가 읽히다 안 읽히다 합니다. 머리띠는 글자가 앉는 왼쪽에
  옅은 막을 깔아 대비를 고정했고(고대비 화면에서는 검은 막 + 밝은 글자),
  카드 설명은 짙은 판(`--ov`) 위에 흰 글자로 얹습니다.
  **사진 밝기에 기대어 글자를 얹지 마세요.**
- **지도 위에 겹쳐 놓는 것은 자리를 못 박지 마세요.** 범례와 '가까운 곳'
  카드가 각자 절대 위치로 붙어 있었고 카드의 `top` 이 "범례 한 줄 높이"인
  46px 로 못 박혀 있었습니다. ③은 종류가 여섯이라 범례가 늘 두세 줄이 되어
  **카드가 범례 글자 위로 올라탔습니다**(넓은 화면에서도). 이제 둘 다
  `.mapleft` 한 상자에 담겨 세로로 쌓입니다. 지도 아래쪽(축척 막대·겹쳐
  그리기 줄·고른 곳 카드)도 좁은 화면에서 층을 나눠 둡니다.
  새로 얹을 것이 생기면 **자리를 px 로 못 박지 말고 그 줄에 넣으세요.**
- **휴대전화에서 세로 가운데 정렬을 쓰지 마세요.** ①의 첫 화면과 ③의 시작
  화면이 남는 공간 가운데에 놓여 있었는데, 휴대전화에서는 위아래로 280px 씩
  비어 **화면이 고장 난 것처럼** 보였습니다(사용자 지적). 좁은 화면에서는
  `align-items:flex-start` 로 위에 붙입니다.
- **가로로 미는 띠에 중요한 값을 숨기지 마세요.** ②③의 요약 띠를 한 줄로
  두고 옆으로 밀어 보게 했더니, 390px 화면에서 **450px 이 화면 밖에** 있었고
  하필 잘리는 자리가 `가장 가까운 곳 ○○ 3.0km` 의 거리였습니다. 줄바꿈으로
  되돌렸습니다(`tests/mob` 이 가로 상자에 숨은 양을 잽니다).
- **★ 좁은 화면에서 위도·경도는 반드시 같은 줄입니다.** `.mb-accrow` 를
  **격자(grid)로 못 박아** 이렇게 놓습니다 — `[찍기][내 위치]` / `[위도][경도][×]`.
  예전에는 그냥 `flex-wrap` 이라 브라우저가 남는 자리대로 접어서 **위도는 첫
  줄, 경도는 둘째 줄**로 갈라졌습니다(사용자 지적 — "보기가 안 좋다").
  한 쌍인데 따로 놓이면 값이 두 군데 있는 것처럼 읽힙니다.
  561px 이상에서는 한 줄로 폅니다(안 그러면 '다시 찍기'가 353px 까지 늘어남).
  `tests/mob` 의 `COORDROW` 가 두 칸의 `top` 을 대조합니다.
- **★ 지도 도구는 첫 화면에 지도가 보여야 합니다.** 상단 바·조건 줄·요약 띠가
  465px 을 먹어 390×664 화면에서 **지도가 199px 밖에 안 보였습니다**(사용자
  지적 — "지도를 보는 화면이 너무 작아서 답답하다"). 이렇게 되찾았습니다.
  - 조건 줄 261 → 170px : '조건 더보기'를 **검색칸 옆**으로(order), '사고지점'
    이름표를 줄 위가 아니라 **왼쪽 옆**으로(grid), 검색칸 48 → 42px
  - 요약 띠 : `margin-left:auto` 가 줄바꿈 flex 에서 **혼자 한 줄**을 만듭니다 —
    ③의 '다시 정하기'가 그래서 50px 을 먹고 있었습니다. `order` 로 앞 조각
    뒤에 붙였습니다
  - 지도 높이 `56vh → 70vh`
  - 결과: 지도가 첫 화면에 **199 → 294px**(②) 보이고, 지도 자체도 372 → 465px
  - `tests/mob` 의 `MAPSEEN` 이 **170px 미만이면 실패**시킵니다.
    ③이 ②보다 낮은 것은 '찾는 조건 바꾸기' 띠가 하나 더 있기 때문입니다.
- **★ ①의 '지도에서 찾기' 창은 목록 칸이 눌리지 않아야 합니다.**
  `.shmap-mapwrap{min-height:44vh}` 가 `.shmap-body` 의 몫 나누기를 이겨서,
  조건 줄·경고 띠가 길어지면 **목록이 0px 로 눌리고 아래 단추와 겹쳤습니다.**
  높이는 grid 가 정하게 두고(`min-height:0`), 조건 줄은 두 줄로 접었습니다
  (시·도/시·군·구 이름표는 화면에서만 비켜 두고 고르는 칸이 스스로
  '시·도 선택'이라고 말합니다 — `assets/shmap.js`).
  `tests/mob` 이 창을 열어 목록·지도 칸이 눌렸는지 잽니다.
- **★ 설명서는 글이 아니라 그림입니다.** 사용자 요청 — "글이 너무 많다.
  최대한 글이 적고 스크린샷이나 그림으로." 예전 판은 **5,910자·10쪽**이었고
  지금은 **1,421자·8쪽**입니다(원페이퍼는 1,349 → 760자 · 한 쪽 그대로).
  - **③ 이 세 걸음이 되면서 사진을 다시 찍었습니다(2026-09-08).**
    걸음 2 사진이 **두 장**입니다(업체 갈래 / 물품 이름) — 두 갈래는 묻는
    것이 달라 한 장만 실으면 다른 갈래를 고른 사람이 자기 화면과 다르다고
    봅니다. 결과 쪽에는 **미리 협의된 곳**(초록 띠)과 **지도 범례 두 개**를
    그림으로 넣었습니다.
  - **나란히 놓는 사진은 옆의 빈 자리를 잘라냅니다**(`칸만`). 화면 전체를
    찍어 A4 폭의 절반으로 줄이면 글자가 3pt 가 되어 읽을 수 없습니다.
  - **표지 사진은 화면 전체**입니다. 카드 줄만 잘라 실었더니 진입 화면이
    사진으로 가득 찬 지금 모양과 달라 보였습니다.
  - `.rz-nav` 는 늘 화면 아래에 붙어 있어(sticky) 그대로 찍으면 사진
    **한가운데**를 가로지릅니다 — 찍는 동안만 `position:static` 으로 세웁니다.
  - **가리킬 자리는 사진 안에 ①②③ 으로 박습니다** — `build/make_guide.mjs`
    의 `딱지`. 설명서 쪽에서 좌표로 얹으면 화면을 조금만 고쳐도 딱지가
    엉뚱한 곳을 가리킵니다. 여기서 실제 요소를 재어 박으면 늘 맞습니다.
    · 낮은 요소(단추·입력칸·띠)는 **왼쪽 밖**에 답니다 — 안쪽에 달면
      '지도에서 ② 찍기' 처럼 글자를 갈라 놓습니다.
    · 어느 경우든 화면 밖으로 나가지 않게 붙잡습니다(잘라 찍는 사진에서
      딱지가 반쯤 잘렸습니다).
  - **한 줄 설명은 여덟 낱말 안쪽**입니다. 문장으로 늘이지 마세요.
    `make_guide.mjs` 가 글자 수를 세어 **2,200자를 넘으면 알려 줍니다.**
  - **사진이 세로로 길면 안 됩니다** — A4 한 쪽에 사진이 둘 들어가야 하는데
    화면 전체를 그대로 찍으면 한 장이 쪽을 다 먹습니다(실제로 그래서
    9쪽이 됐습니다). `찍기` 에 자를 네모를 넘길 수 있습니다.
  - **이 환경에서만 뜨는 띠는 가립니다** — 배경지도 경고, 그리고 예시값
    (`○○화학`) 때문에 뜨는 주의 띠. 실제로 쓰면 안 뜨는 것이라, 사진에
    남겨 두면 읽는 사람이 자기 화면이 잘못된 줄 압니다.
- **⚠ "인터넷이 끊겨도 열리게" 하는 기능(서비스워커)은 뺐습니다. 되살리지
  마세요 — 되살리려면 실제 배포 주소에서 먼저 확인해야 합니다.**
  Cloudflare Pages 에 올렸더니 진입 화면은 뜨는데 **하위 화면
  (`res/index.html` 등)으로 들어가면 `ERR_FAILED` 로 끊겼습니다.** 원인을
  셋 찾아 고쳤는데(아래) **네 번 올려도 그대로였습니다.** 시범운영에 필요한
  것은 "링크를 누르면 도구가 열리는 것"이지 끊겼을 때도 열리는 것이
  아니므로, 그 기능을 뺐습니다. 인터넷 없는 자리는 **망분리용 단일 파일**이
  원래부터 담당합니다 — 없어진 기능이 아닙니다.
  - `sw.js` 는 **지우지 말고 그대로 두세요.** 지금은 아무 일도 하지 않고
    **스스로를 지우는** 파일입니다. 이미 예전 판을 열어 본 사람의 브라우저에
    고장 난 것이 설치되어 있어서, 파일을 지우기만 하면 그 사람들은 계속
    고장 난 것을 봅니다. `assets/pwa.js` 도 같은 일을 한 번 더 합니다.
  - **고쳤는데도 안 되면 "고친 것이 적용되지 않은 것"부터 의심하세요.**
    서비스워커는 탭을 전부 닫기 전까지 옛 것이 계속 맡습니다. 이것 때문에
    고친 판을 두 번이나 올리고도 화면이 그대로였습니다.
  - 되살릴 때 함께 고쳐야 하는 세 가지는 아래에 남겨 두었습니다.
- **⚠ 시범운영 링크는 지금 닫혀 있습니다(2026-09-03).** 정식 안내 전에
  링크가 여러 곳으로 퍼져 사람들이 수시로 들어오고 있다고 해서 사용자가
  막아 달라고 했습니다. **임의로 다시 열지 마세요** — 여는 것은 사용자가
  정합니다. 절차는 `docs/링크막기.md`.
  - 깃허브 Pages(`…github.io/nics_chem-safety-cheatkey/`)는 워크플로의
    `SITE_OPEN: "no"` 한 줄로 닫았습니다. 도구를 **아예 안 올리고**
    `build/닫힘안내.html` 한 장 + `robots.txt` 만 올립니다.
    **워크플로 파일을 지우는 것으로는 안 막힙니다** — 자동 배포만 멈추고
    이미 올라간 사이트는 그대로 살아 있습니다.
  - Cloudflare(`chem-safety-kr.pages.dev`)는 **사용자만** 막을 수 있습니다.
    이 개발 환경은 `dash.cloudflare.com` 으로 나갈 수 없습니다.
  - 닫아 두는 동안에도 **죽은 링크로 두지 않습니다.** 퍼진 링크로 들어온
    사람이 오류 화면을 보면 담당자에게 전화가 옵니다 — 안내문 한 장이
    "왜 닫혔고 어떻게 문의하는지"를 대신합니다. 안내문에 **연락처를
    지어 넣지 않았습니다**(2절 3항).
  - **주소는 버리지 않습니다.** 이미 퍼진 주소라, 다시 열 때 같은 주소를
    써야 저장해 둔 사람이 찾아옵니다. Cloudflare 프로젝트를 지우면 그
    이름을 남이 가져갈 수 있습니다.
- **바탕화면 아이콘은 그대로 됩니다** — 매니페스트·아이콘·아이폰 태그는
  남아 있습니다(서비스워커와 무관). 아이폰은 공유 → '홈 화면에 추가',
  PC·안드로이드는 브라우저 메뉴로 깝니다. 다만 안드로이드가 스스로 권하는
  설치 팝업은 서비스워커가 있어야 뜨는 경우가 있어 안 뜰 수 있습니다.
  시범운영에 링크가 도는 것이 먼저라고 보고 그쪽을 택했습니다.
- **(참고 · 예전 기록) 웹앱을 넣을 때의 조건들** — 시범운영에 링크 하나를
  나눠 주려고 넣었습니다(사용자 요청 — "압축파일을 뿌리는것보다 링크를 하나
  건내주고 싶어… 바탕화면에 아이콘으로"). **기존 사용법을 하나도 바꾸지 않는
  것**이 조건이었습니다.
  - `manifest.webmanifest` 와 `sw.js` 는 **사이트 뿌리**에 둡니다. 서비스워커는
    자기가 놓인 폴더까지만 맡아서, `assets/` 안으로 내리면 `sms/`·`map/`·`res/`
    가 관할 밖이 되어 도구 세 개가 오프라인에서 안 열립니다.
  - 주소는 **상대경로(`./`)** 로만 적습니다. 뿌리(`/`)로 적으면 하위 폴더에
    올린 순간(깃허브 Pages 가 그렇습니다) 아이콘·설치가 통째로 깨집니다.
  - **`file://` 에서는 아무 일도 하지 않습니다.** `pwa.js` 가 https·localhost
    일 때만 등록합니다. 망분리용 단일 파일에는 `build_single.py` 의
    `strip_pwa()` 가 아예 빼고 만듭니다(옆에 파일이 없어 404 가 납니다).
  - **탭 아이콘만은 주소 안에 그림을 그대로 적었습니다**(`data:image/svg+xml`).
    파일을 더 부르지 않아서 `file://` 에서도 단일 파일에서도 똑같이 보입니다.
  - 캐시는 **먼저 저장된 것을 보여 주고 뒤에서 새것을 받는** 방식입니다.
    자료를 갈아 끼운 직후 **한 번은 이전 자료가 보일 수 있습니다** — 그래서
    화면마다 자료 기준일을 적어 두는 것이 더 중요해졌습니다. 한 번에 확실히
    갈아 끼우려면 `sw.js` 의 `var 버전` 을 올립니다.
  - `sw.js` 의 `PRECACHE` 가 화면이 부르는 것과 어긋나면 끊긴 자리에서 도구가
    반쪽만 열립니다. 눈으로는 못 찾으므로 `tests/pwa` 가 네 HTML 을 긁어
    대조합니다. **화면에 파일을 새로 붙이면 그 목록에도 넣으세요.**

  **실제로 올려 보고 네 번 깨졌습니다 — 그래서 기능을 뺐습니다**
  Cloudflare Pages 에 처음 올렸을 때 진입 화면은 뜨는데 **하위 화면
  (`res/index.html` 등)으로 들어가면 `ERR_FAILED` 로 끊겼습니다.** 사진 같은
  하위 자원은 멀쩡했습니다 — 화면 이동(`mode:"navigate"`) 요청만 겪는
  문제였습니다. 원인은 셋이고 셋 다 함께 고쳐야 합니다.
  - **Cloudflare Pages 는 `/res/index.html` 을 `/res/` 로 정리해 돌려줍니다**
    (리다이렉트). 그것을 따라간 응답은 `redirected` 가 `true` 인데, 화면
    이동 요청에 그런 응답을 돌려주면 크롬이 **"주소창과 실제로 받아 온 곳이
    다르다"며 연결 자체를 거부합니다.** 그래서 `redirected` 면 몸통·상태·
    헤더만 옮겨 **새 `Response` 를 만들어** 돌려줍니다.
  - **미리 받아 둘 때도 같은 처리를 해야 합니다.** 한 줄짜리 `cache.add()`
    는 그 딱지가 붙은 채로 저장해 버려서, 나중에 저장본을 그대로 돌려주기만
    해도 똑같이 끊깁니다. 실시간 받기 쪽만 고쳤다가 **두 번째 배포도
    그대로 실패했습니다.** `fetch` 로 받아 딱지를 지우고 `cache.put` 합니다.
  - **`self.skipWaiting()` 이 있어야 고친 것이 적용됩니다.** 서비스워커는
    기본적으로 그 사이트를 열어 둔 탭이 **전부 닫힐 때까지** 옛 것이 계속
    맡습니다. 옛 것이 고장 나 있으면 고친 판을 올려도 사용자는 계속 고장 난
    것을 봅니다 — **세 번째 배포가 그대로였던 이유입니다.**
  이 셋을 다 고치고도 **네 번째 배포가 그대로였습니다.** 더 파는 대신
  기능을 뺐습니다 — 사용자가 시범운영 링크를 기다리는 중이었고, 되는지
  확신 못 하는 기능 때문에 링크가 안 열리는 것은 앞뒤가 바뀐 일입니다.

  **`tests/pwa` 는 이제 반대를 검사합니다** — 서비스워커가 화면을 맡지
  않는지, 예전에 깔린 것이 지워지는지, 그리고 **리다이렉트를 실제로 돌려주는
  서버에서 카드를 눌러 세 도구에 들어가지는지**. 검증이 통과했는데 실제로는
  깨져 있던 경험이라, 이제 사용자가 하는 동작(카드 누르기·주소 직접 치기)을
  그대로 해 봅니다.
- **아이콘은 방패 안의 사고지점 표시입니다**(`build/icon.svg`).
  처음에는 **경고 삼각형(!)** 이었는데 사용자가 "오류 마크 같아서 불편한
  느낌"이라고 했습니다 — 실제로 그 모양은 어디서나 **오류·경고**로 읽히고,
  바탕화면에 늘 놓고 보는 아이콘이 "무언가 잘못됐다"고 말하면 안 됩니다.
  지금은 방패(안전) 안에 가운데 점 + 원(지도의 사고지점 마커와 같은 구성)
  입니다 — 경고가 아니라 **대응**을 뜻합니다.
  - **정부상징(태극)을 넣지 않았습니다.** 기관의 공식 표장을 앱 아이콘에
    넣는 것은 "공식 앱"이라는 주장이라 기관 승인이 필요합니다.
    GHS 그림문자(마름모)도 쓰지 않았습니다 — 법으로 뜻이 정해진 표지라
    도구 아이콘으로 쓰면 없는 뜻을 주장하게 됩니다.
  - 바꾸려면 `build/icon.svg` 를 고치고 `node build/make_icons.mjs`.
    ⚠ **네 HTML 의 탭 아이콘(`data:image/svg+xml`)도 같이** 바꿔야 합니다 —
      그것은 주소 안에 그림이 적혀 있어 이 파일을 보지 않습니다.
- **설치 안내는 진입 화면 맨 아래에만 둡니다.** 위쪽에 얹으면 머리띠가
  높아져 휴대전화에서 셋째 카드가 화면 밖으로 밀립니다(전에 실제로 그랬음).
  일하는 화면(도구 셋)에는 아예 넣지 않습니다 — 일과 상관없는 것입니다.
  기기마다 방법이 달라 **그 기기에 맞는 것 하나만** 넣습니다(안드로이드·PC 는
  단추, 아이폰은 공유 메뉴 안내). 이미 앱으로 열었으면 아무것도 넣지 않습니다.
- **좌표가 없는 자료 줄은 지도에 넣지 않습니다.**
- 마커: 초록 **비상구 그림** 화학사고 대피장소 / 보라 **집 그림** 이재민
  임시주거시설 / 빨강 사고지점·영향반경 / 파랑 내 위치.
  위험 표시가 자료 종류보다 우선합니다.
  - **두 층은 색만이 아니라 모양으로도 갈립니다**(2026-09-07 사용자 요청 —
    "이재민임시주거시설 api도 주민대피장소와 구별되도록 아이콘 사용하자").
    그림은 `map/app.js` 의 `SH_ICON` — 대피장소는 **비상구(문 + 나가는
    화살표)** = "잠깐 피하는 곳", 임시주거시설은 **집(지붕 + 문)** =
    "머무는 곳". 색을 못 가리는 담당자도 구별됩니다.
  - ⚠ **어느 쪽이 진짜 대피처라는 뜻이 아닙니다** — 두 자료는 별개의 층이고
    판단은 담당자가 합니다(위 항목). 그림도 '무엇을 하는 곳'만 가리키고
    우열을 매기지 않습니다.

---

## 8. 지금 상태와 다음에 할 일

**지금까지** — 세 도구 모두 동작하고, 배포 폴더·망분리용 단일 파일까지 나옵니다.
검증 19묶음 전부 통과. 진입 화면은 사용자가 그려 온 **개선(안)** 대로 다시 짜고,
사용자가 자리마다 골라 준 **실사 사진 8장**(머리띠 5 + 카드 3)을 넣었습니다.
머리띠는 사진과 문구가 **5초마다 함께 바뀝니다**(7절).
**시범운영용 링크 배포 준비도 끝냈습니다** — 바탕화면 아이콘·웹앱 설치·
인터넷 없이 열기까지(7절). 어디에 올릴지는 `docs/배포하기.md`.

사용자가 KRDS 스킬(`krds-chemical-safety-government-web`)을 건네주었습니다.
진입 화면을 고칠 때 그 안의 `references/components-and-forms.md`(카드 위계·CTA·
반응형)와 `references/photo-direction.md`(사진 방향), `references/qa-checklist.md`
(마무리 점검)를 봤습니다. 스킬과 사용자 요구가 부딪히면 **사용자 요구가 먼저**
입니다(스킬 §1.2) — 호버로 설명을 드러내는 것이 그 예이고, 대신 접근성이 깨지지
않도록 7절의 세 가지 조건을 함께 지켰습니다.

디자인 개편은 사용자와 이렇게 정했습니다(`preview/design.html` 3안 중에서).
- **A안 뼈대** — 세 도구 전부 KRDS 표준형으로. 색·글자·크기 전면 교체 ✔ 했음
- **고대비(어두운) 화면** — 켜고 끄는 단추로 ✔ 했음 (`assets/theme.js`)
- **① 문자 도구만 C안 흐름**(한 걸음씩 단계형)으로 ✔ 했음
- 주로 쓰는 화면은 **사무실 PC + 현장 휴대전화** 둘 다

**① 문자 도구가 어떻게 바뀌었나** (`sms/app.js` 의 `stepList`~`showStep`)
- 걸음은 발송 구분에 따라 만들어집니다 —
  `사고유형 → 발송 정보 → 사고 발생 위치 → 문자 받을 지역 → 사고물질`
  (+ 대피 구분이면 `대피장소`) `→ 문안 확인`. 6~7걸음입니다.
- **빈칸이 남으면 문안을 아예 만들지 않습니다.** 마지막 걸음에 "아직 채우지
  않은 칸이 N개" 목록과 그 걸음으로 가는 단추(`.needfill`)가 대신 나옵니다.
  예전에는 `{시각}` 이 그대로 남은 반쪽 문안이 보였습니다 — 그걸 그대로
  복사해 발송할 위험이 있어 없앴습니다.
- 진행 표시(`.stepbar`)를 눌러 아무 걸음으로나 건너뜁니다. 급할 때 아는 값부터
  채우는 사람도 있고, ② 지도에서 값을 들고 오면 이미 채워진 걸음도 있습니다.
- 묶음 번호(`FIELD_GROUPS.번호`)는 화면에 더 이상 안 씁니다 — 진행 표시가
  번호를 매기는데 묶음이 또 매기면 같은 칸이 번호 두 개를 갖게 됩니다.
- **6걸음의 '지도에서 찾기'는 앞 걸음에 적은 주소 자리에서 열립니다.**
  ② 지도에서 넘어온 좌표(`state.acc`)가 있으면 그것을, 없으면 시·군·구·읍·면·동
  으로 어림잡아(`MAPCORE.placeAt`) 사고지점을 찍고 가까운 순으로 정렬합니다.
  어림값이면 창 위에 붉은 줄로 근거를 적습니다(`.shmap-guess`).
  `SHMAP.open({사고지점:{lat,lon,어림,근거}})` 로 넘깁니다.
- **선택 항목 문법** — 템플릿에 `[[키:…]]`(값이 있을 때) ·`[[!키:…]]`(비었을 때)
  를 쓸 수 있습니다(`sms/app.js` 의 `applyChunks`). `FIELDS` 에서 `req` 를 두지
  않은 칸은 비어도 문안을 만듭니다.
- **진입 화면** ✔ 했음 — KRDS 표준형으로 만든 뒤, 사용자가 그려 온 개선(안)에
  맞춰 다시 짰습니다. 자바스크립트를 허용해 고대비 단추도 있습니다(예전에는
  JS 없는 것이 설계였고 `tests/t9` 가 그것을 검사했는데, 그 검사도 새로 썼습니다).

  화면 뼈대(`index.html`) — `.top` 상단 바 · **`.cover`**(안에 `.hero-ph` 사진 ·
  `.hero` 글 · `<main>` 카드) · `footer`. 사진이 `.cover` 바로 아래에 있는
  이유와 격자로 칸을 덮는 방식은 7절을 보세요.

  카드 한 장의 구성(`index.html` · `assets/portal.css`)
  - `.pn-ph` 사진 칸(16:9) — 안에 `.pn-art`(사진 · `aria-hidden` · `alt=""`)와
    `.pn-ov`(설명)가 겹쳐 있습니다. **`.pn-ov` 는 `.pn-art` 밖에 있어야**
    화면낭독기가 읽습니다.
  - `.pn-lbs`/`.pn-lb` 사진 위 딱지 — 차례로 나타났다 사라집니다.
    자리(`left`/`top` %)는 사진에 찍힌 그것 옆에 맞춰 둡니다. `top` 은
    **85% 를 넘기지 마세요** — 딱지 높이만큼 사진 칸 아래로 삐져나갑니다.
  - `.pn-hd` `01 이름` 한 줄 · `.pn-foot` 자료 건수 + 둥근 화살표(`.pn-go`).
  - 올리면: 테두리 파랑 + 살짝 떠오름 + 그림 1.03배 + 화살표가 파랗게 참 +
    설명이 올라옴. `:hover` 와 `:focus-visible` 을 **한 선택자 줄에 묶지 않습니다**
    — `:focus-visible` 을 모르는 낡은 브라우저가 줄 전체를 버려 호버까지 죽습니다.
  - 자료 건수의 날짜처럼 중간에서 끊기면 나쁜 토막은 `<span class="nb">` 로
    묶습니다(붙임표에서 줄이 바뀌어 "2026-05-" / "19" 로 갈라졌습니다).

**KRDS 로 옮기면서 놓쳤다가 고친 것 — 같은 실수를 되풀이하지 마세요**
- `--line-d`·`--glass`·`--route`·`--me`·`--warn-b` 같은 이름을 **고대비
  모드에만** 정의해, 밝은 화면에서 그 이름을 쓰는 줄이 통째로 버려졌습니다.
  입력칸 테두리가 아예 없었습니다(CSS 는 없는 이름을 만나면 조용히 그 줄을
  버립니다 — 화면은 멀쩡해 보입니다). 이제 `tests/tok` 이 스타일시트에서
  쓰는 모든 이름을 긁어 **두 모드에서 값이 있는지** 검사합니다.
  → 색·크기 이름을 새로 만들 때는 **밝은 `:root` 와 고대비 블록 양쪽에**
    넣고 `tests/run.sh tok` 을 돌리세요.
- 세로 flex 안에서 `margin:0 auto` 만 주면 브라우저가 늘리기를 포기해
  상자가 내용 크기로 쪼그라듭니다. `width:100%` 를 함께 줘야 합니다.
- `.mb-f input[type=text]` 같은 전역 폼 규칙이 좁은 화면 미디어쿼리에서 다시
  나오므로, 그 안에 든 좁은 칸(`.mb-accrow`)은 **선택자를 한 단 깊게** 써야
  이깁니다(`.mb-acc .mb-accrow input`). 안 그러면 휴대전화에서 좌표칸이
  한 줄씩 차지합니다.
- 지도 위에 겹쳐 그리는 표시(사고지점 십자·반경 원)는 `pointer-events:none`
  이어야 합니다. 그 아래 마커를 못 누르게 됩니다(실제로 그랬음).
- `<img width height>` 가 붙어 있으면 브라우저가 **`aspect-ratio` 를 무시합니다**
  (한쪽이 `auto` 일 때만 씁니다). `height:auto` 를 빼먹어 사진 칸이 원본 높이
  750px 로 길어졌습니다.

**베낀 코드가 죽어 있던 것 — ③ 도구 안 검색**
`res/app.js` 의 `placeIndex()` 가 ② 에서 그대로 옮겨져 **대피장소 줄 배열**을
훑도록 되어 있었는데, ③ 의 `resTree()` 는 값이 `1` 인 표(어느 시·도에 어느
시·군·구가 있는가)입니다. 그래서 글자마다 `rows.forEach is not a function` 이
나 **도구 안 검색이 통째로 죽어 있었습니다** — 인터넷이 될 때만 결과가 나왔고,
사업장·장비 이름으로는 아무것도 안 나왔습니다. 지금은 `allRes()` 를 훑고,
보유 내용(`c`)·운영기관·자원 종류까지 견줍니다(`tests/res2` 가 검사).
→ **화면끼리 코드를 옮겨 붙일 때는 자료 구조가 같은지 먼저 확인하세요.**

**★ 바로 다음 — 인증키에 IP 를 등록해야 받아집니다**

받아 오는 길은 셋 다 만들어 두었습니다(브라우저 페이지 · 파이썬 · 깃허브
액션). 액션에서 실제로 불러 봤더니 서버가 **`32 UNREGISTERED IP ERROR`** 로
답했습니다 — 이 오픈API 는 **활용신청 때 적어 둔 IP 에서만** 받아집니다.
그래서 남은 것은 **사용자가 그 IP 를 등록하고 그 자리에서 한 번 여는 것**
입니다(또는 플랫폼에서 파일로 내려받아 넘겨 주기). 넣으면 ② 지도에
이재민 임시주거시설 층이 바로 생깁니다(코드는 다 되어 있고, 만 곳 규모까지
`tests/run.sh tmpbig` 으로 미리 재 두었습니다).
그다음 `tests/run.sh tmp2` 로 확인하고 `build_single.py` 를 다시 돌리세요.

- **자료 주소가 바뀌었습니다(2026-09-07).** 사용자가 재난안전데이터공유플랫폼
  인증키를 받아 와서 `www.safetydata.go.kr/V2/api/DSSP-IF-10945` 로 바꿨습니다.
  예전 것(`api.odcloud.kr/…uddi:78c724b6-…`)은 **시·도별 집계표**였습니다 —
  17개 시·도 × 시설구분 3종 = 51줄에 개소·면적·수용능력만 있고 **시설의
  이름·주소·좌표가 없어** 지도에 찍을 수 없었습니다(2026-08-13 확인).
  **그 주소로 되돌리지 마세요.**
- **새 자료가 목록인지도 아직 확인하지 못했습니다.** 그 서버로 나갈 수 없어
  한 줄도 못 받아 봤습니다. 집계표를 또 받은 경우에는 페이지가 **왜 안 되는지
  띄우고 파일을 만들지 않습니다**(`tests/fetchpage` 가 검사).
- **파일로 받아 붙여 주는 길이 더 확실합니다** —
  `python3 build/make_tempshelters.py 받은것.json`. 그 길이면 칸 이름을
  제가 직접 보고 맞출 수 있습니다.

그 파일이 없는 동안에는 ② 지도에 그 층이 나오지 않고 화면은 예전 그대로입니다
(정상 동작입니다 — 고장이 아닙니다).

**아직 남은 것**
- **⚠ 사용자가 Cloudflare 에서 눌러야 닫힙니다.** `chem-safety-kr.pages.dev`
  는 사용자가 올린 것이고 이 환경은 그리로 못 나갑니다. 안내문 한 장
  (`build/닫힘안내.html` 을 `index.html` 로 바꿔 폴더째)을 같은 자리에
  다시 올리면 도구가 서버에서 사라집니다 — `docs/링크막기.md` 2절.
  Cloudflare 화면의 단추 이름은 **확인하지 못했습니다**(바깥 접속 차단).
  사용자가 화면을 찍어 주면 그 자리에서부터 알려 주세요.
- **다시 열 때** — `python3 build/make_release.py` 로 만든
  `release/화학사고_초동대응_지원_서비스/` 폴더를 같은 자리에 올립니다.
  주소는 그대로 씁니다(퍼진 주소라 바꾸면 저장해 둔 사람이 못 찾습니다).
  그때 할 일: ① 브이월드 인증키의 활용 도메인에 그 주소를 등록하고 새 키를
  `data/basemap.js` 에 넣기 ② 주소 이름을 바꿨다면 `docs/배포하기.md` 의
  안내문·등록 주소와 **설명서 PDF 두 가지의 주소**도 함께 고치기.
- **★ 방제자원은 두 갈래입니다 — `업체 섭외` / `방제물품 찾기`
  (2026-09-07 사용자 분류안 PPT).** 사용자가 PPT 로 분류 체계를 정해
  주었습니다. **③ 을 다시 짜기 전에 `docs/방제자원_정리/통합/README.md`
  를 읽으세요.**
  ```
  방제자원
  ├─ ① 업체 섭외   수집·운반 / **중간처분** / 종합재활용 /
  │                최종처분(매립) / 중장비 / **독성가스** / **폐수 수탁처리**
  │   필수 4항목: 허가 현황 · 처리가능물질 종류 · 연락처 · 주소
  │   (폐기물 4갈래는 **허가증상 처리가능 지정폐기물 목록**이 필수)
  └─ ② 방제물품    물품 이름을 고르면 **그것만** 보여야 함
      필수 4항목: 자원명 · 보유 개수 · 보유 업체·기관명 · 담당자 연락처
  ```
  - **권역 5개 + 시·도**로 나눕니다 — 수도권 / 강원권역 / 충청권역 /
    경상권역 / 전라권역. ⚠ **제주는 분류안에 없어 전라권역에 넣었습니다**
    (영산강청 관할 · 표 09 의 묶음을 따름) — 제 판단이라 확인이 필요합니다.
  - 만드는 법: `python3 build/방제자원_통합.py` (표 19개 → `통합/업체.csv`
    1,717줄 · `통합/물품.csv` 4,075줄) → `python3 build/방제자원_통합_보기.py`
    (눌러 보는 검토 페이지).
  - **⚠ 중간처분은 소각·중화를 나누지 않고 하나입니다**(2026-09-07 사용자 —
    "중간처분은 다 합치도록 하자"). 분류안(PPT)은 따로 그렸지만 급할 때
    고르는 것은 "처분을 맡길 곳" 하나이고, 소각인지 중화인지는 `허가현황`
    칸에 **원문이 그대로** 남아 있습니다. 다시 쪼개지 마세요.
  - **⚠ 화면에 "협의 완료" 라고 적지 마세요** — 담당자가 그 말의 뜻을
    모릅니다(2026-09-07 사용자). `협의권역` 이 있는 줄은 **갈래마다 맨 위**
    에 오고, 초록 띠 + **「먼저 연락 · 미리 협의된 곳」** 딱지로 드러냅니다.
    무엇을 하라는 것인지가 딱지에 그대로 있어야 합니다.
  - **한 업체가 여러 줄일 수 있습니다** — 허가를 여럿 가진 곳이 많습니다
    ((주)엔아이티는 네 갈래). 합칠 때 열쇠에 **갈래를 넣습니다**. 업체
    이름만으로 합치면 한 갈래만 남아 "폐산을 중화할 곳"을 찾는 사람에게
    그 업체가 안 보입니다.
  - **독성가스와 폐수 수탁처리는 각각 별도 갈래입니다**(2026-09-07 사용자
    지시 — "독성가스는 업체 섭외 항목에 독성가스라는 칸으로 추가",
    폐수 수탁처리는 "다른갈래로 만들어줘"). 폐기물 여섯 갈래에 **섞지
    마세요** — 폐수 수탁처리는 물환경보전법 허가라, 섞으면 담당자가
    "이 업체가 폐기물도 받는다"고 잘못 읽습니다.
  - **제주는 전라권역**입니다 — 2026-09-07 사용자가 확인해 주었습니다.
  - **★ 권역은 소재 + 협의 둘 다입니다(`찾을권역`).** 업체가 **있는 곳**과
    **맡기로 한 곳**이 다를 수 있습니다 — (주)세송유화텍은 충북 진천에
    있는데 강원권을 맡고("강원권에 업체가 없어서 가까운 충북을 넣은 것"),
    씨이케이는 여수에 있는데 경상권도, (주)상록은 충청권도 맡습니다.
    사용자가 **"둘다 넣어줘"** 라고 정했습니다(2026-09-07). 한쪽만 쓰면
    다른 쪽 담당자가 그 업체를 영영 못 찾습니다. 화면의 권역 고르기는
    합집합(`찾을권역`)을 쓰고, 두 권역인 줄에는 딱지를 붙입니다.
    ⚠ `찾을권역만들기()` 는 **두 인자 모두 쪼갭니다** — 합칠 때 이미
    "·" 로 이어 붙인 값이 다시 들어와서, 안 쪼개면 같은 권역이 여러 번
    붙습니다(실제로 그랬습니다).
  - **⚠ 물품 이름은 표준 27가지입니다** — 원자료가 한 표에서만 484가지였고,
    처음에 84가지로 묶었더니 **칩이 여섯 줄**이 되어 훑을 수가 없었습니다
    (2026-09-07 사용자 — "너무 가지수가 많아지는데 좀 더 가짓수를 줄여서").
    묶는 기준은 **급할 때 한 번에 요청하는 단위**입니다 — 방독면·정화통·
    공기호흡기는 "호흡보호구" 하나로 부릅니다. 원래 이름은 각 줄의
    `원래이름` 에 남습니다. 규칙은 `방제자원_통합.py` 의 `물품사전` 한 곳.
    · **뜻이 다른 것을 크기 때문에 합치지 마세요** — 흡착포와 중화제를
      합치면 "폐산을 중화할 것"을 찾는 사람이 흡착포를 받습니다.
    · 못 묶은 표기는 **하나의 `기타`** 로 모읍니다. 표기마다 칩을 만들면
      "방제장비"·"소형"·"처리능력" 같은 칸이 25개 늘어서는데, 그 이름으로는
      아무것도 고를 수 없습니다.
  - **★ 허가증 24건을 읽어 표 17 을 만들었습니다(2026-09-07).** 사용자가
    `0. 허가증 전체.zip` 을 직접 보내 주었습니다. **전부 스캔 이미지**라
    글자를 못 뽑아 쪽마다 그림으로 확인했습니다. 허가증 1쪽에 분류안의
    필수 네 항목이 다 있습니다(⑤ 전문처리분야 · ⑥ 영업대상폐기물 ·
    ④ 주소·전화 · 허가번호·허가기관).
    - **`중간처분(중화)` 가 확인되었습니다** — (주)엔아이티의 *지정폐기물
      중간처분업(화학적 처분)* 허가증에 **폐산·폐알칼리**로 적혀 있습니다.
      전국취합 자료로는 못 가려내던 갈래입니다.
    - **12건은 "붙임 참조"로만** 적혀 있고 본문이 최대 39쪽입니다.
      사용자 지시대로 그 줄은 **허가 종류만** 적고 `확인못함` 칸에 이유를
      남겼습니다 — 지어내지 않습니다.
    - 같이 온 연락처 엑셀에서 **업체명·직장전화·주소·권역·갈래만** 썼습니다.
      **성명·휴대폰·이메일은 쓰지 않았고 저장소에도 넣지 않았습니다.**
    - `협의권역` 은 **그 업체가 맡기로 한 권역**이라 소재지와 다를 수
      있습니다(씨이케이는 여수에 있으나 경상권도 맡음). 화면의 권역은
      주소로 정한 **소재 권역**을 씁니다.
  - **⚠ 처리가능 지정폐기물 목록이 1,003줄에 없습니다** — 분류안이 필수라고
    못 박은 항목인데, 가장 넓은 표 14(전국취합 1,511행)에 **그 칸이 아예
    없습니다.** 표 03·13·17 에서 업체명이 같은 것만 옮겨 채웠습니다(589줄).
  - **⚠ 드라이브 자료를 직접 못 읽습니다** — `방제자원_정리` 의 9건이 모두
    zip(37~137MB)과 155MB PDF 입니다. 드라이브 도구는 base64 로만 돌려주어
    이 크기는 대화로 옮길 수 없습니다. **사용자가 zip 안의 엑셀을 파일로
    붙여 주는 길이 유일합니다**(6절과 같은 이유).
  - 독성가스 회수업체 2곳은 분류안 여섯 갈래에 없어 `특수처리` 로 따로
    두었습니다 — 어디에 넣을지 사용자가 정합니다.
- **③ 방제자원 — 나머지 DB 를 정리해 두었습니다(반영은 아직 안 함).**
  드라이브 `방제자원_전체` 를 표 18개(자료 9,226행)로 펴서
  `docs/방제자원_정리/` 에 넣었습니다. 서비스 자료(`data/`)는 **건드리지
  않았습니다** — 사용자가 "정리만 하고 반영은 나중에"라고 했습니다.
  - 지금 470건 → **지도에 올릴 수 있는 것 1,947곳**(약 4.1배).
    `hwp`·구형 `xls` 까지 전부 열어 **"건수만 아는 자료"는 없습니다.**
  - **넣기 전에 풀어야 하는 것**: 좌표가 하나도 없음(주소뿐) · 자료 기준일이
    기관마다 다름(2024-12 ↔ 2026-01) · 업종 표기 19가지 · 연락처 결측 19%.
  - **자원 5배가 되면 지금 화면으로는 못 씁니다** — 기본 반경 30km·종류별
    상위 몇 곳만 펴기를 함께 해야 합니다. 방안은 `반영방안.md`.
  - **`06_독성가스_취급사업장`(1,579곳)은 자원이 아니라 위험시설입니다.**
    자원 목록에 섞지 마세요. **2026-09-03 사용자 결정으로 이 표는 통째로
    보류**입니다 — "방제자원으로 보기 어렵다". 검색 후보로 쓰는 방안도
    당분간 하지 않습니다(방안만 `반영방안.md` 5절에 남겨 둠).
  - ⚠ 원자료에 **담당자 실명 2,719건·개인 휴대전화 495건**이 있었습니다.
    정리본에서 전부 걷어냈고(`99_개인정보_제거현황.csv`), **원문은 저장소에
    넣지 않았습니다.** 다시 받으려면 `00_원자료_목록.csv` 의 파일 ID 로.
  - **못 읽은 것은 155MB PDF 한 권뿐입니다.** hwp·hwpx 6건과 구형식 xls
    1건은 파일을 내려받아 전부 열었습니다(`build/hwp읽기.py`, xlrd).
    그 결과 **폐기물처리업체 전국취합 1,505행**(표 14)과 **전남·광주
    방제약품 11곳**(표 15)이 새로 들어왔습니다.
  - ⚠ **'가스 회수' 갈래가 2건인 것은 파일 탓이 아니라 자료에 원래 두
    곳뿐이기 때문입니다**(㈜해성화공·㈜동양화학 · 표 16). 늘리려면 자료
    자체를 더 받아야 합니다 — 예전에 "hwp 를 못 읽어서"라고 적었던 것은
    제 잘못된 짐작이었습니다.
  - **폐기물 자료가 두 갈래로 들어와 있습니다** — 전국취합본(표 14 ·
    1,123곳)과 유역청별 회신 건수(표 12 · 1,669곳). 기준일과 범위가 달라
    수가 다릅니다. **둘을 다 넣으면 겹칩니다** — 하나만 고르세요(표 14 권장).
  - 표 15(전남·광주 방제약품)는 **자료 기준이 2017년**입니다.
  - **사용자가 파일을 직접 건네주면 그 길이 훨씬 정확합니다.**
    드라이브 도구는 표를 줄바꿈 없는 한 줄로 돌려줘 행을 되돌려야 하는데,
    파일은 `build/xlsx읽기.py`(표준 라이브러리만 씀)로 칸 그대로 읽습니다.
    받은 파일은 `방제자원_보충` 환경변수가 가리키는 폴더에 둡니다 —
    개인정보가 들어 있어 **저장소에는 넣지 않습니다.**
- **★ 원내 자료 3건을 공부해 '무엇이 방제자원인가'를 확인했습니다
  (2026-09-03).** 정리는 `docs/원내자료_공부와_최종방향.md` —
  **③ 을 고치기 전에 반드시 읽으세요.** 요점만 옮기면:
  - **방제자원 = 장비 · 인력 · 물품**(원내 공식 정의). **인력이 들어갑니다** —
    `반영방안.md` 의 '인력 지원' 갈래는 넣는 것이 맞습니다.
    목표는 **잔류폐기물 3시간 이내 초동대응 완료**.
  - **표준 조사서식(안)**: 자원유형 **①차량·장비 ②방제물품 ③중화·처리제
    ④보호구 ⑤선박 ⑥처리시설** + **대응 물질군 A·A′·B·C·D·E(복수, 매칭의
    핵심)** + 신설 항목 4가지(취약시간 가용여부 · 출동 소요시간 실측 ·
    동원 조건 · 최종 현행화일 — **미갱신은 회색 표시**).
  - **3단계 동원 사다리**: 1순위 **0~30분** 긴급동원 13개社·화학안전공동체 /
    2순위 **30분~2시간** 합동방재센터·지자체·소방·해경·수질센터 /
    3순위 **2시간 이후** 폐기물·독성가스회수·해양방제·중장비업체.
  - **'26.11 에 원내 사고상황공유앱에 방제자원 DB 가 올라갑니다.**
    우리 도구와 보는 사람이 달라 겹치지 않지만, **자료의 속을 표준에 맞춰
    두어야** 그때 갈아끼울 수 있습니다 — 이번 방향의 한 줄은
    **「속은 표준대로, 겉은 담당자 말로」** 입니다. 화면의 7갈래는
    사용자가 정한 것이라 **그대로 둡니다**.
  - **대피장소 공식 정의는 "최소한의 선정기준 · 지자체장이 탄력적으로
    적용"** 입니다 — 도구가 대피장소를 판정하면 안 되는 근거가 공식 문서에
    있습니다. **'26년 지정 1,849개소** = 우리 `shelters.js` 와 일치.
    공동활용 1,167 · 표지판 1,235(우리 자료엔 없음).
    ⚠ **500m 이격은 화면에 넣지 마세요** — 그것은 *지정할 때* 취급시설에서
    재는 값이라, 사고지점에서 재어 보여 주면 뜻이 달라집니다.
  - **대피 안내문구 필수 항목**(업체명·주소·시간·우회도로·집결지·대피장소)을
    **① 이 이미 여섯 가지 다 갖췄습니다.** ② 의 영향 참고 반경도 공식
    "ERG 안전거리 우선 제공"과 같은 방식입니다. 되돌리지 마세요.
  - **아직 우리가 정하면 안 되는 것**: 물질 460종의 **물질군 태깅**.
    원내 분류표('26.8 배포)를 받아야 합니다 — 지어내면 **틀린 중화제를
    권하게** 됩니다. **30/60/120분 시간권역**도 출동시간 실측치가 없어
    지금 만들면 어림값을 실측처럼 보이게 합니다.
- ③ 사업장 전화번호가 비어 있는 8곳(해경 3 · 유역환경청 4 · 해성화공 1).
  검색으로 확인이 안 돼 비워 뒀습니다. 사용자 확인이 필요합니다.
- ①·②에도 ③처럼 "처음엔 한 가지만 묻는" 시작 화면을 둘지 — 아직 안 정했습니다.
- `README.md` 맨 아래 `초안 단계에서 확인이 필요한 사항` 7가지(글자수 산정,
  기관명 위치, 경계 자료 라이선스 등)는 원내 확인이 필요합니다.

---

## 9. 일하는 방식

- 커밋 메시지는 한국어로, **왜 고쳤는지**를 적습니다. 검증 결과도 한 줄 넣습니다.
- 푸시는 위에 적은 작업 가지로만 합니다. PR 은 사용자가 요청할 때만 만듭니다.
- 화면을 바꿨으면 스크린샷을 찍어 사용자에게 보여 줍니다. 사용자는 개발자가
  아니므로 **무엇을 눌러야 하는지까지 쉬운 말로** 설명합니다.
- 자료·수치는 지어내지 말고, 확인이 안 되면 "확인 못 했다"고 말합니다.
